---
schema_version: 2
id: concept.단어-임베딩
page_type: concept
title: 단어 임베딩
aliases:
  - Word Embedding
  - word vector
  - 분산 단어 표현
  - distributed word representation
tags:
  - type/concept
  - domain/ai
  - domain/nlp
  - domain/machine-learning
created: '2026-07-18'
updated: '2026-07-18'
lifecycle: active
verification: verified
artifacts:
  - 'raw/035_Neural Probabilistic Language Model - Distributed Word Representations and Neural Language Modeling.ko.md'
  - 'raw/035_Neural Probabilistic Language Model - Distributed Word Representations and Neural Language Modeling.commentary.ko.md'
evidence:
  - source_id: bengio-et-al-2003-nplm
    locator: 'JMLR 3, pp. 1139–1143의 distributed word feature 정의·공동 학습과 pp. 1150–1152의 한계·미래 과제'
    relation: supports
related:
  - source.035
  - concept.신경-확률-언어-모형
  - concept.n-gram-모델
  - concept.데이터-희소성
  - concept.순환-신경망
  - concept.대규모-언어-모델
  - source.031
  - analysis.n-gram에서-llm으로
---
# 단어 임베딩

단어 임베딩(word embedding)은 어휘의 각 단어를 낮은 차원의 조밀한 실수 벡터에 대응시키는 표현이다. Bengio 등의 2003년 [[신경 확률 언어 모형]]에서는 어휘 행렬 $C\in\mathbb R^{|V|\times m}$의 각 행을 다음 단어 예측과 함께 학습했다.

## 분산 표현

one-hot 표현은 단어마다 서로 직교하는 좌표를 사용하지만, 임베딩은 $m\ll |V|$인 여러 연속 특징을 모든 단어가 공유한다. 비슷한 문맥에서 다음 단어 예측에 비슷한 역할을 하는 단어들은 학습 중 가까운 벡터를 얻을 수 있다.

`분산`은 한 개념을 한 노드·한 차원에만 저장하지 않고 여러 차원의 활성 패턴으로 나타낸다는 뜻이다. 각 차원 하나를 반드시 사람이 이름 붙일 수 있는 의미 속성으로 해석할 수 있다는 뜻은 아니다.

## 정적 word type 표현

2003년 $C(w)$는 같은 표기 단어에 항상 같은 벡터를 주는 **정적 임베딩**이다. 문맥의 다른 단어들과 MLP가 결합해 다음 단어 확률은 달라지지만, `bank`의 금융·강둑 의미마다 별도 벡터를 주지는 않는다. 원 논문은 polysemous word에 여러 점을 주는 방안을 미래 과제로 남겼다.

현대 contextual embedding은 같은 token도 주변 문맥에 따라 층별 hidden state가 달라진다. 입력 lookup embedding과 Transformer의 contextual representation을 모두 “임베딩”이라고 부를 수 있지만 계산 단계와 의미가 다르다.

## 무엇을 학습하는가

언어 모델링 목적은 실제 다음 단어의 로그확률을 높인다. gradient는 문맥 단어의 벡터를 예측에 유용한 방향으로 바꾼다. 결과 벡터에는 의미·통사·빈도·말뭉치 편향이 함께 담길 수 있다.

벡터 거리가 가까운 단어가 항상 유의어인 것은 아니다. 반의어도 비슷한 문맥에 나타나 가까울 수 있고, 고빈도·형태·주제 효과가 거리를 지배할 수 있다. analogy·유사도·downstream 성능은 별도 평가해야 한다.

## LSA·word2vec·GloVe와의 관계

[[잠재 의미 분석]]은 문서–용어 행렬을 절단 SVD해 단어·문서를 저차원 공간에 놓는다. NPLM은 순차 다음 단어 가능도에서 lookup vector를 gradient로 학습한다. 둘 다 연속 저차원 표현이지만 입력 통계와 목적이 다르다.

word2vec은 CBOW·skip-gram이라는 단순 예측 구조와 hierarchical softmax·negative sampling을 사용했고, GloVe는 전역 동시출현 비율을 회귀한다. 이들은 분산 표현이라는 문제를 공유하지만 Bengio MLP의 전체 정규화 언어 모형과 동일하지 않다.

## 한계

- 고정 단어 어휘는 OOV를 처리하지 못하며 희귀어 벡터의 추정도 불안정하다.
- word type당 한 벡터는 다의성과 문맥 변화를 한 점에 섞는다.
- 말뭉치의 사회적 편향과 빈도 불균형이 공간에 반영될 수 있다.
- 기하적 유사성이 사실 지식, 조합 의미와 문장 이해를 자동 보장하지 않는다.
- 벡터의 회전·재매개변수화 때문에 개별 차원 의미는 대개 식별되지 않는다.

## 출처

- [[035_신경 확률 언어 모형과 분산 단어 표현]]
- Yoshua Bengio·Réjean Ducharme·Pascal Vincent·Christian Jauvin, [A Neural Probabilistic Language Model](https://www.jmlr.org/papers/v3/bengio03a.html), JMLR 3, 2003, §§1.1·2·5.2.

## 관련 항목

- [[035_신경 확률 언어 모형과 분산 단어 표현]]
- [[신경 확률 언어 모형]]
- [[N-gram 모델]]
- [[데이터 희소성]]
- [[순환 신경망]]
- [[대규모 언어 모델]]
- [[031_잠재 의미 분석과 확률적 잠재 의미 색인]]
- [[N-gram에서 LLM으로]]
