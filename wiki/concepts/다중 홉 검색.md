---
schema_version: 3
id: concept.다중-홉-검색
page_type: concept
title: 다중 홉 검색
aliases:
  - Multi-hop Retrieval
  - Multi-hop Reasoning
  - 다중 홉 추론
tags:
  - type/concept
  - domain/ai
  - domain/nlp
  - domain/computer-science
created: '2026-07-18'
updated: '2026-07-21'
editorial_status: active
review:
  evidence_coverage: verified
  content_mode: descriptive
artifacts:
  - raw/046_Memory Networks External Memory for Neural Question Answering.ko.md
  - raw/046_Memory Networks External Memory for Neural Question Answering.commentary.ko.md
evidence:
  - source_id: weston-chopra-bordes-2014-memory-networks
    locator: §3.1 equations (2)–(3)의 첫째·둘째 supporting memory 선택과 §5.2 simulated world QA
    relation: supports
  - source_id: sukhbaatar-et-al-2015-end-to-end-memory-networks
    locator: 초록과 §§2–3의 recurrent attention hop과 표현 갱신
    relation: contextualizes
relations:
  - target: source.046
    kind: related
  - target: concept.메모리-네트워크
    kind: related
  - target: concept.외부-메모리
    kind: related
learning:
  difficulty:
    entry: intermediate
    target: intermediate
  prerequisites:
    - target: concept.개방-영역-질의응답
  assumed_knowledge: 없음
  outcomes:
    - '첫 검색 결과로 다음 질의나 상태를 갱신해 여러 사실을 잇는 흐름을 설명하고, 검색 경로와 논리적으로 올바른 추론을 구분할 수 있다.'
  next:
    - target: analysis.검색-근거-독해-답
      reason: '다음에는 검색은 근거를 찾고 독해는 답을 찾는다에서 검색과 답 추출의 오류를 나눠 보고, 신경 정보 검색에서 학습된 표현으로 후보를 찾는 방식을 살핀다.'
    - target: concept.신경-정보-검색
      reason: '다음에는 검색은 근거를 찾고 독해는 답을 찾는다에서 검색과 답 추출의 오류를 나눠 보고, 신경 정보 검색에서 학습된 표현으로 후보를 찾는 방식을 살핀다.'
---
# 다중 홉 검색

> [!note] 학습 안내
> **난이도:** 중급<br>
> **선수 지식:** [[concept.개방-영역-질의응답|개방 영역 질의응답]]<br>
> **읽고 나면:** 첫 검색 결과로 다음 질의나 상태를 갱신해 여러 사실을 잇는 흐름을 설명하고, 검색 경로와 논리적으로 올바른 추론을 구분할 수 있다.

## 1단계 — 먼저 잡을 핵심

[[다중 홉 검색]]은 한 번의 검색 결과를 다음 검색 질의나 상태 갱신에 사용해 여러 관련 항목을 순차적으로 찾는 방식이다. 질문에 답하려면 서로 다른 사실을 연결해야 할 때 사용한다.

## 2단계 — 작동 원리

### 첫 결과에서 다음 검색으로

질문으로 첫 관련 항목을 찾고, 그 결과를 질문 상태에 결합해 다음 항목을 찾는다. 필요한 근거를 모을 때까지 이 과정을 반복한 뒤 최종 답을 고른다.

## 3단계 — 기술과 근거

### Memory Network의 두 홉

2014년 MemNN은 질문과 가장 잘 맞는 첫 supporting memory를 고른 뒤, 질문과 첫 메모리를 함께 사용해 둘째 supporting memory를 골랐다. 예를 들어 물체를 누가 놓았는지와 그 사람이 어느 방에 있었는지를 이어 물체 위치를 답했다.

이 구조의 원 실험은 최대 두 hard `argmax` 선택과 supporting-fact 라벨을 사용했다. 2015년 End-To-End Memory Network는 soft attention 결과를 질문 표현에 더하고 다음 홉을 반복했다.

## 검증과 한계

### 검색과 추론의 경계

여러 문서를 순서대로 찾았다는 사실만으로 논리적으로 올바른 추론을 보장하지 않는다. 각 홉의 후보 재현율, 질의 갱신, 관계 방향, 시간·부정·수량 조건과 최종 답의 충실성을 따로 평가해야 한다. “multi-hop reasoning”이라는 이름이 인간의 내적 사고 과정이나 명시적 논리 증명을 뜻하지도 않는다.

홉을 늘리면 더 긴 사슬을 찾을 가능성과 함께 오류 누적·계산 비용·무관한 경로도 늘어난다. 필요한 홉 수를 고정할지 동적으로 멈출지도 설계 문제다.

## 학습 확인

1. 다중 홉 검색은 한 번의 검색 결과를 다음 단계에 어떻게 사용하는가?
2. Memory Network의 두 홉은 첫 supporting memory와 둘째 supporting memory를 어떤 순서로 선택하는가?
3. 여러 항목을 찾았다는 사실이 올바른 논리 추론을 자동 보장하지 않는 이유는 무엇인가?

다음에는 [[검색은 근거를 찾고 독해는 답을 찾는다]]에서 검색과 답 추출의 오류를 나눠 보고, [[신경 정보 검색]]에서 학습된 표현으로 후보를 찾는 방식을 살핀다.

### 다음 문서

- [[analysis.검색-근거-독해-답|검색은 근거를 찾고 독해는 답을 찾는다]] — 다음에는 검색은 근거를 찾고 독해는 답을 찾는다에서 검색과 답 추출의 오류를 나눠 보고, 신경 정보 검색에서 학습된 표현으로 후보를 찾는 방식을 살핀다.
- [[concept.신경-정보-검색|신경 정보 검색]] — 다음에는 검색은 근거를 찾고 독해는 답을 찾는다에서 검색과 답 추출의 오류를 나눠 보고, 신경 정보 검색에서 학습된 표현으로 후보를 찾는 방식을 살핀다.

## 출처
- [[046_메모리 네트워크와 외부 지식 접근]]
- Jason Weston·Sumit Chopra·Antoine Bordes, [Memory Networks](https://arxiv.org/abs/1410.3916), 2014, §3.1과 §5.2.
- Sainbayar Sukhbaatar 외, [End-To-End Memory Networks](https://proceedings.neurips.cc/paper_files/paper/2015/hash/8fb21ee7a2207526da55a679f0332de2-Abstract.html), 2015.

## 관련 항목

- [[analysis.검색-근거-독해-답|검색은 근거를 찾고 독해는 답을 찾는다]]
- [[concept.신경-정보-검색|신경 정보 검색]]
- [[concept.개방-영역-질의응답|개방 영역 질의응답]]
- [[source.046|메모리 네트워크와 외부 지식 접근]]
- [[concept.메모리-네트워크|메모리 네트워크]]
- [[concept.외부-메모리|외부 메모리]]
