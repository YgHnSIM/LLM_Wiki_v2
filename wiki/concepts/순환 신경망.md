---
schema_version: 3
id: concept.순환-신경망
page_type: concept
title: 순환 신경망
aliases:
  - Recurrent Neural Network
  - Recurrent Neural Networks
  - RNN
  - 순환형 신경망
tags:
  - type/concept
  - domain/ai
  - domain/machine-learning
  - domain/nlp
created: '2026-07-16'
updated: '2026-07-21'
editorial_status: active
review:
  evidence_coverage: verified
  content_mode: descriptive
artifacts:
  - raw/026_Recurrent Neural Networks - Machines That Remember.ko.md
  - raw/026_Recurrent Neural Networks - Machines That Remember.commentary.ko.md
evidence:
  - source_id: williams-zipser-1989-recurrent-learning
    locator: 'pp. 272–275, §§2.1–2.4, 특히 p. 274의 real-time caveat'
    relation: supports
  - source_id: elman-1990-finding-structure-in-time
    locator: pp. 179–185와 Figures 1–2
    relation: supports
  - source_id: werbos-1990-bptt
    locator: '초록과 §§I–III, pp. 1550–1555'
    relation: supports
  - source_id: williams-peng-1990-online-bptt
    locator: 초록과 pp. 490–501
    relation: supplements
  - source_id: bengio-simard-frasconi-1994-long-term-dependencies
    locator: '초록과 §§II–IV, pp. 157–164'
    relation: supports
  - source_id: hochreiter-schmidhuber-1997-lstm
    locator: '초록과 §§1–3, pp. 1735–1745'
    relation: supplements
  - source_id: cho-et-al-2014-rnn-encoder-decoder
    locator: '§§2.2–2.3와 equations (5)–(8), pp. 1725–1726'
    relation: supplements
  - source_id: bahdanau-cho-bengio-2015-attention
    locator: 초록과 §1의 고정 길이 벡터 병목
    relation: contextualizes
  - source_id: vaswani-et-al-2017-attention
    locator: '초록과 §§1, 3.5, 4 및 Table 1'
    relation: contextualizes
  - source_id: gpt-2018
    locator: '§§2–3, 특히 Figure 1'
    relation: contextualizes
  - source_id: bert-2019
    locator: §3.1과 Figure 1
    relation: contextualizes
relations:
  - target: source.026
    kind: related
  - target: source.028
    kind: related
  - target: concept.역전파
    kind: related
  - target: concept.기울기-소실
    kind: related
  - target: concept.시간-지연-신경망
    kind: related
  - target: concept.대규모-언어-모델
    kind: related
  - target: concept.은닉-마르코프-모델
    kind: related
learning:
  difficulty:
    entry: advanced
    target: advanced
  prerequisites:
    - target: concept.다층-퍼셉트론
    - target: concept.연쇄-법칙과-계산-그래프
  assumed_knowledge: 없음
  outcomes:
    - 'RNN의 상태 갱신·시간축 가중치 공유·BPTT를 설명하고 상태와 기억, 후속 순서열 구조의 차이를 구분할 수 있다.'
  next:
    - target: concept.신경망-기계-번역
      reason: 다음에는 신경망 기계 번역에서 순환 인코더와 디코더가 가변 길이 순서열을 연결하는 방식을 살핀다.
    - target: concept.장단기-메모리
      reason: 장기 의존성을 위한 게이트 구조는 장단기 메모리에서 이어 본다.
---
# 순환 신경망

> [!note] 학습 안내
> **난이도:** 심화<br>
> **선수 지식:** [[concept.다층-퍼셉트론|다층 퍼셉트론]], [[concept.연쇄-법칙과-계산-그래프|연쇄 법칙과 계산 그래프]]<br>
> **읽고 나면:** RNN의 상태 갱신·시간축 가중치 공유·BPTT를 설명하고 상태와 기억, 후속 순서열 구조의 차이를 구분할 수 있다.

## 1단계 — 먼저 잡을 핵심

### 핵심 정의

[[순환 신경망]](Recurrent Neural Network, RNN)은 이전 시점의 상태를 현재 계산에 다시 입력하는 신경망 계열이다. 같은 상태 전이 매개변수를 시간 위치마다 공유해 가변 길이 순서열을 처리한다. `재귀 신경망`이라는 번역은 나무 구조에 같은 함수를 적용하는 recursive neural network와 혼동될 수 있어 이 문서에서는 사용하지 않는다.

## 2단계 — 작동 원리

### 순환 상태와 계산식

단순한 Elman형 RNN은 다음처럼 쓸 수 있다.

\[
h_t=\phi(W_{xh}x_t+W_{hh}h_{t-1}+b_h)
\]

\[
o_t=g(W_{ho}h_t+b_o)
\]

$h_t$는 이전 상태 $h_{t-1}$와 현재 입력 $x_t$의 학습된 함수다. 따라서 과거 입력은 현재 상태를 거쳐 뒤의 출력에 영향을 줄 수 있다. 출력 $o_t$는 선택적이며, 모든 시점에서 출력하거나 마지막 상태만 분류에 쓰는 등 과제에 따라 구성이 달라진다.

Jordan형 순환망은 출력 또는 별도 상태 단위를 되먹임했고, Elman의 단순 순환망은 은닉 활성을 한 시점 늦춰 context unit에 복사했다. 오늘날 `RNN`은 이런 역사적 구조 하나만의 고유 이름이 아니라 recurrent state를 가진 여러 모델의 상위 범주다.

### 시간축 공유와 펼치기

$W_{xh}$·$W_{hh}$·$W_{ho}$는 시간 $t$가 달라져도 같은 값이 사용된다. 이 시간축 매개변수 공유 덕분에 길이마다 별도 모델을 만들지 않고 같은 전이 규칙을 반복 적용할 수 있다. 한 시간 위치에서 학습한 변환을 다른 위치에도 재사용한다는 귀납 편향도 생긴다.

접힌 그림의 순환 간선을 길이 $T$의 입력에 대해 펼치면 $h_0\rightarrow h_1\rightarrow\cdots\rightarrow h_T$인 유한한 비순환 계산 그래프가 된다. 각 시점의 셀은 복사본처럼 보이지만 매개변수는 공유한다. $h_t$가 $h_{t-1}$에 의존하므로 한 순서열의 시간 위치를 모두 동시에 계산하기는 어렵지만, 배치·행렬 연산과 은닉 단위 수준의 병렬화는 가능하다.

### BPTT 학습

시간을 통한 역전파(backpropagation through time, BPTT)는 펼친 그래프에 일반 [[역전파]]를 적용한다. 한 시점의 $h_t$는 $h_{t-1}$과 현재 입력의 합성 함수이므로, 시간축을 거꾸로 지날 때는 [[연쇄 법칙과 계산 그래프]]의 국소 도함수 곱이 반복된다. 손실이 여러 시점에 있으면 각 시점의 기여를 합하고, 같은 매개변수가 여러 시점에서 사용됐으면 그 사용 지점들의 그래디언트도 합산한다.

전체 이력을 펼치면 계산량과 중간 활성화 메모리가 길이에 따라 증가한다. Williams·Peng의 truncated BPTT는 일정 구간만 역전파해 비용을 낮추지만, 구간 밖의 먼 학습 신호를 직접 자르는 근사다. Williams·Zipser의 RTRL 미분은 가중치를 고정한 궤적에는 시간 의존성을 자르지 않은 정확한 도함수를 전방으로 계산한다. 그러나 이를 사용해 매 시점 가중치를 바꾸면 이후 상태 궤적도 달라져 전체 궤적 목적의 정확한 음의 그래디언트를 더는 따르지 않고, 작은 학습률에서 가까운 근사가 된다. BPTT와는 정확성의 조건, 계산 방향·비용이 다르다.

### 상태와 기억의 차이

은닉 상태를 `기억`이라고 부르는 것은 과거가 현재 계산에 영향을 준다는 비유다. $h_t$는 앞선 입력 전체의 복사본이나 데이터베이스가 아니라, 과제 손실에 맞춰 학습된 유한 차원 표현이다. 무엇을 유지할지는 모델 용량, 상태 전이, 활성화 함수, 데이터와 최적화에 달린다.

먼 과거에 수학적으로 의존할 수 있는 **표현 가능성**, 실제 훈련이 그 의존성을 발견하는 **학습 가능성**, 상태에서 입력 이력을 되살리는 **복원 가능성**은 서로 다르다. vanilla RNN의 상태 하나가 모든 관련 정보를 보존하거나 문법·의미를 자동으로 이해한다고 가정하지 않는다.

## 3단계 — 기술과 근거

### 장기 의존성과 게이트 구조

긴 시간축의 그래디언트는 시점별 야코비안의 연속 곱을 포함한다. 관련 방향이 반복해서 작아지면 [[기울기 소실]]이, 커지면 폭주가 나타날 수 있다. 가중치 행렬의 고윳값 하나만이 아니라 비선형 활성화의 도함수와 실제 상태 궤적을 포함한 전체 곱이 결과를 결정한다.

[[장단기 메모리|LSTM]]은 셀 상태와 곱셈 게이트를 사용해 장기 오류 흐름과 선택적 갱신을 개선했고, GRU는 reset·update gate로 상태를 갱신하는 더 단순한 recurrent unit을 제시했다. 둘 다 RNN 계열의 구조적 변형이다. 이들이 vanilla RNN보다 긴 의존성을 학습하기 쉽게 만들 수는 있지만 모든 길이·데이터·최적화에서 정보 보존이나 안정적 학습을 보장하지 않는다.

### TDNN·attention·Transformer와의 차이

[[시간 지연 신경망]]은 고정된 시간 창에 공유 필터를 적용하는 순방향 구조로, 이전 은닉 상태를 다음 시간 단계로 되먹임하지 않는다. 수용 영역 안의 여러 위치를 병렬 계산할 수 있지만 한 출력이 볼 수 있는 입력 범위는 설계한 유한한 창과 층 구조로 정해진다.

초기 RNN encoder–decoder는 입력 전체를 마지막 encoder state 하나에 압축했다. [[신경망 기계 번역]]의 attention은 출력 시점마다 입력 위치별 은닉 표현을 가중 결합해 이 고정 벡터 병목을 완화했다. Transformer는 recurrence를 유지한 채 attention을 덧붙인 RNN이 아니라, 핵심 순서열 연산에서 순환을 제거하고 self-attention과 위치 표현을 사용하는 구조다.

GPT·BERT 같은 Transformer 언어 모델도 RNN식 단일 상태 갱신을 사용하지 않는다. RNN 언어 모델과 GPT가 자기회귀 조건부 확률 분해를 공유할 수 있다는 사실은 과업 수준의 공통점이며, [[대규모 언어 모델]]이 RNN 계산을 직접 상속했다는 뜻은 아니다.

## 검증과 한계

### 적용 범위와 한계

가변 길이 입력에 같은 상태 전이를 적용할 수 있다는 사실은 유한 차원 상태가 과거 전체를 완전히 보존한다는 뜻이 아니다. 긴 시간축에서는 반복되는 야코비안 곱 때문에 그래디언트가 소실되거나 폭주할 수 있으며, 게이트·clipping·attention도 모든 데이터에서 장기 의존성 학습을 보장하지 않는다.

## 학습 확인

1. RNN은 현재 입력과 이전 상태로 무엇을 계산하며 어떤 매개변수를 시간마다 공유하는가?
2. 시간축으로 펼친 계산 그래프에서 BPTT는 공유 매개변수의 그래디언트를 어떻게 모으는가?
3. 은닉 상태를 과거 전체의 저장본으로 보거나 attention·Transformer를 같은 순환 연산으로 볼 수 없는 이유는 무엇인가?

다음에는 [[신경망 기계 번역]]에서 순환 인코더와 디코더가 가변 길이 순서열을 연결하는 방식을 살핀다. 장기 의존성을 위한 게이트 구조는 [[장단기 메모리]]에서 이어 본다.

### 다음 문서

- [[concept.신경망-기계-번역|신경망 기계 번역]] — 다음에는 신경망 기계 번역에서 순환 인코더와 디코더가 가변 길이 순서열을 연결하는 방식을 살핀다.
- [[concept.장단기-메모리|장단기 메모리]] — 장기 의존성을 위한 게이트 구조는 장단기 메모리에서 이어 본다.

## 출처
- [[026_순환 신경망과 시간적 문맥 학습]]
- Ronald J. Williams·David Zipser, [A Learning Algorithm for Continually Running Fully Recurrent Neural Networks](https://doi.org/10.1162/neco.1989.1.2.270), 1989, pp. 272–275, §§2.1–2.4.
- Jeffrey L. Elman, [Finding Structure in Time](https://doi.org/10.1207/S15516709COG1402_1), 1990, pp. 179–211, 특히 Figures 1–2.
- Paul J. Werbos, [Backpropagation Through Time](https://doi.org/10.1109/5.58337), 1990, pp. 1550–1560.
- Ronald J. Williams·Jing Peng, [An Efficient Gradient-Based Algorithm for On-Line Training of Recurrent Network Trajectories](https://doi.org/10.1162/neco.1990.2.4.490), 1990, 초록과 pp. 490–501.
- Yoshua Bengio·Patrice Simard·Paolo Frasconi, [Learning long-term dependencies with gradient descent is difficult](https://doi.org/10.1109/72.279181), 1994, pp. 157–166.
- Sepp Hochreiter·Jürgen Schmidhuber, [Long Short-Term Memory](https://doi.org/10.1162/neco.1997.9.8.1735), 1997, pp. 1735–1780.
- Kyunghyun Cho 외, [Learning Phrase Representations using RNN Encoder–Decoder](https://aclanthology.org/D14-1179/), 2014, pp. 1724–1734.
- Dzmitry Bahdanau·Kyunghyun Cho·Yoshua Bengio, [Neural Machine Translation by Jointly Learning to Align and Translate](https://arxiv.org/abs/1409.0473), 2015, 초록과 §1.
- Ashish Vaswani 외, [Attention Is All You Need](https://proceedings.neurips.cc/paper_files/paper/2017/hash/3f5ee243547dee91fbd053c1c4a845aa-Abstract.html), 2017, 초록과 §§1·3.5·4.
- Alec Radford 외, [Improving Language Understanding by Generative Pre-Training](https://cdn.openai.com/research-covers/language-unsupervised/language_understanding_paper.pdf), 2018, §§2–3와 Figure 1.
- Jacob Devlin 외, [BERT: Pre-training of Deep Bidirectional Transformers for Language Understanding](https://aclanthology.org/N19-1423/), 2019, §3.1과 Figure 1.

## 관련 항목

- [[concept.신경망-기계-번역|신경망 기계 번역]]
- [[concept.장단기-메모리|장단기 메모리]]
- [[concept.다층-퍼셉트론|다층 퍼셉트론]]
- [[concept.연쇄-법칙과-계산-그래프|연쇄 법칙과 계산 그래프]]
- [[source.026|순환 신경망과 시간적 문맥 학습]]
- [[source.028|장단기 메모리와 장기 의존성 학습]]
- [[concept.역전파|역전파]]
- [[concept.기울기-소실|기울기 소실]]
- [[concept.시간-지연-신경망|시간 지연 신경망]]
- [[concept.대규모-언어-모델|대규모 언어 모델]]
- [[concept.은닉-마르코프-모델|은닉 마르코프 모델]]
