---
schema_version: 2
id: concept.신경망-기계-번역
page_type: concept
title: 신경망 기계 번역
aliases:
  - Neural Machine Translation
  - NMT
  - 뉴럴 기계 번역
tags:
  - type/concept
  - domain/ai
  - domain/nlp
  - domain/machine-learning
created: '2026-05-08'
updated: '2026-07-21'
lifecycle: active
verification: verified
artifacts:
  - raw/022_IBM Statistical Machine Translation - From Rules to Data.ko.md
  - raw/022_IBM Statistical Machine Translation - From Rules to Data.commentary.ko.md
  - 'raw/045_Sequence-to-Sequence Neural Machine Translation End-to-End Learning Revolution.ko.md'
  - 'raw/045_Sequence-to-Sequence Neural Machine Translation End-to-End Learning Revolution.commentary.ko.md'
  - 'raw/050_Subword Tokenization and FastText Character N-gram Embeddings for Robust Word Representations.ko.md'
  - 'raw/050_Subword Tokenization and FastText Character N-gram Embeddings for Robust Word Representations.commentary.ko.md'
  - 'raw/053_Google Neural Machine Translation End-to-End Learning Revolutionizes Translation.ko.md'
  - 'raw/053_Google Neural Machine Translation End-to-End Learning Revolutionizes Translation.commentary.ko.md'
  - 'raw/055_The Transformer Attention Is All You Need.ko.md'
  - 'raw/055_The Transformer Attention Is All You Need.commentary.ko.md'
evidence:
  - source_id: sutskever-vinyals-le-2014-seq2seq
    locator: pp. 3104–3112, 특히 초록과 §§1–3
    relation: supports
  - source_id: bahdanau-cho-bengio-2015-attention
    locator: 초록과 §§1, 3.1, 5.2.1과 6
    relation: supports
  - source_id: cho-et-al-2014-rnn-encoder-decoder
    locator: '§§2–4의 RNN Encoder–Decoder·GRU와 구 기반 SMT의 추가 특징 실험, pp. 1724–1731'
    relation: contextualizes
  - source_id: wu-et-al-2016-gnmt
    locator: '초록과 §§2–5의 8층 attention LSTM·residual·WordPiece·production 평가'
    relation: contextualizes
  - source_id: vaswani-et-al-2017-attention
    locator: pp. 5998–6008, 특히 §§1–3.2와 7
    relation: contextualizes
  - source_id: sennrich-haddow-birch-2016-subword-nmt
    locator: '§§1·3.2와 Algorithm 1의 고정 어휘 희귀어 문제와 BPE 분절, §§4–5의 WMT 2015 평가'
    relation: supplements
  - source_id: google-research-2016-gnmt-production
    locator: '2016-09-27 제품 공지의 중국어→영어 100% 적용과 하루 약 1,800만 건 번역 범위'
    relation: supplements
related:
  - source.045
  - source.053
  - source.055
  - concept.sequence-to-sequence
  - concept.인코더-디코더
  - concept.자기회귀-생성
  - source.022
  - source.026
  - concept.기계-번역
  - concept.규칙-기반-기계-번역
  - concept.통계적-기계-번역
  - concept.지도-학습
  - concept.순환-신경망
  - concept.장단기-메모리
---
# 신경망 기계 번역

> [!note] 학습 안내
> **난이도:** 중급<br>
> **선수 지식:** [[순환 신경망]], [[인코더-디코더]]<br>
> **읽고 나면:** NMT가 원문 조건부 번역 확률을 신경망으로 학습하는 흐름과 seq2seq·어텐션·Transformer의 차이를 설명할 수 있다.

## 1단계 — 먼저 잡을 핵심

### 핵심 정의

[[신경망 기계 번역]](Neural Machine Translation, NMT)은 신경망이 원문을 조건으로 번역문 토큰의 확률 분포를 학습하는 [[기계 번역]] 접근이다. 2014년 Sutskever·Vinyals·Le의 seq2seq 연구는 [[순환 신경망]] 계열인 다층 [[장단기 메모리|LSTM]] 인코더와 디코더로 영어→프랑스어 번역을 수행했고, 2015년 Bahdanau·Cho·Bengio는 고정 길이 벡터의 병목을 줄이기 위해 내용 기반 어텐션을 결합했다.

## 2단계 — 작동 원리

### 번역 생성 흐름

인코더는 원문 토큰들을 연속 표현으로 바꾸고, 디코더는 그 표현과 앞서 생성한 토큰을 조건으로 다음 번역 토큰의 분포를 계산한다. 초기 seq2seq는 원문 전체를 고정 벡터에 압축했고, 어텐션은 매 출력 시점에 필요한 입력 위치들의 표현을 다시 가중 결합했다.

## 3단계 — 기술과 근거

### 2014년 두 연구의 범위

Sutskever 등의 시스템은 4층 LSTM으로 원문을 고정 벡터에 넣고 번역문을 직접 생성했다. Cho 등의 RNN Encoder–Decoder는 뒤에 GRU라고 불린 새 게이트 유닛을 제안했지만, 대표 실험은 신경망이 계산한 구 쌍 점수를 기존 구 기반 SMT에 특징으로 넣었다. 두 연구가 [[Sequence-to-Sequence 학습]]의 일반 틀을 함께 정착시켰다는 설명과 완전히 같은 종단간 번역 시스템이었다는 주장을 구분한다.

### seq2seq 조건부 번역

이 초기 seq2seq 구성은 한 LSTM이 원문을 고정 길이 벡터로 부호화하고 다른 LSTM이 그 벡터를 조건으로 번역문을 한 토큰씩 생성했다. 이는 모든 RNN이 마지막 상태 하나만 사용한다는 뜻도, 번역을 비확률적 신경망 계산으로 바꿨다는 뜻도 아니다. 모델은 연쇄 법칙에 따라 `P(y|x)`를 다음 토큰 조건부 확률들의 곱으로 나타내고 최대 가능도 학습과 탐색을 사용했다.

2014년 실험은 WMT-14 영어→프랑스어 과제에서 LSTM 단독 번역과 구 기반 SMT 후보 재순위화를 구분해 보고했다. 특정 데이터와 평가의 결과를 모든 언어쌍에서 NMT가 즉시 우월했다는 결론으로 확대하지 않는다.

### 어텐션과 정렬

Bahdanau 등의 어텐션은 번역 토큰을 생성할 때 인코더의 입력 위치별 표현에 연속 가중치를 계산한다. 어느 입력이 현재 출력과 관련되는지를 나타낸다는 점에서는 [[통계적 기계 번역]]의 단어 정렬과 기능적으로 비교할 수 있다. 그러나 IBM 정렬은 생성 모형의 이산 잠재 변수이고, 신경망 어텐션은 현재 은닉 상태에서 미분 가능하게 계산되는 연속 가중치다. IBM EM 정렬이 어텐션으로 직접 변형됐다는 역사적 인과는 이 유사성만으로 입증되지 않는다.

### Transformer의 번역 구조

2017년 [[Transformer]]는 [[순환 신경망|순환망]] 대신 self-attention과 위치 표현을 사용해 번역을 학습했다. 원 구조는 6층 encoder와 6층 decoder, multi-head attention, 위치별 feed-forward network, 잔차 연결과 Post-LayerNorm을 결합했다. 이는 어텐션 기반 NMT의 후속 구조이지만 seq2seq LSTM이나 IBM Models와 동일한 연산은 아니다.

WMT 2014 영어→독일어·영어→프랑스어에서 경쟁력 있는 BLEU와 8개 P100 GPU 기준 base 12시간·big 3.5일 훈련을 보고했다. 훈련 위치를 병렬 계산하는 장점은 분명하지만 표준 self-attention의 길이 제곱 비용과 자기회귀 decoder의 토큰별 생성 순차성은 남는다.

2016년 GNMT는 2014년 고정 벡터 모델을 그대로 제품화한 구성이 아니라 8층 LSTM 인코더·디코더, 어텐션, [[잔차 연결]], WordPiece와 별도 빔 탐색 보정을 결합했다. 맨 아래 인코더 층만 양방향이고 위층은 단방향이며, 어텐션은 최상위 인코더 상태와 첫 디코더 층 사이에 배치됐다. 이는 깊은 번역 모델을 여러 장치에서 훈련·서빙하려는 시스템 설계까지 포함한다.

논문은 영어↔프랑스어·스페인어와 영어↔중국어의 production phrase-based system을 비교했고, 고립된 단순 문장에 대한 인간 side-by-side 평가에서 평균 번역 오류가 약 60% 감소했다고 보고했다. 이 평균을 모든 언어·문서의 보편적 품질 향상으로 읽지 않는다. 2016년 9월 공식 제품 공지가 확인하는 첫 100% 적용 범위는 중국어→영어, 하루 약 1,800만 건이었다. 논문의 다언어 평가와 제품의 최초 전환 범위를 구분한다.

### SMT·규칙 기반 접근과의 차이

[[규칙 기반 기계 번역]]은 사전·문법·변환 규칙을 주로 사람이 명시한다. [[통계적 기계 번역]]은 병렬 자료에서 어휘·정렬·구 대응 확률을 추정하되 언어 모델, 번역 모델과 재배열 특징을 분리한다. NMT는 이 구성 요소의 상당 부분을 신경망의 연속 표현과 조건부 분포 안에서 함께 학습한다.

세 접근은 역사적으로 완전히 단절된 상자가 아니다. 전처리, 어휘 제한, 탐색, 길이 보정, 데이터 정제와 평가 설계는 NMT에도 남고, 초기 NMT 연구는 SMT 후보 재순위화를 함께 실험하기도 했다. 데이터 학습이 인간 설계를 전부 제거했다고 보지 않는다.

## 검증과 한계

### 남는 과제

2014년 seq2seq 실험은 어휘 밖 단어에 불이익을 받았고, 고정 길이 벡터에 원문 전체를 압축했다. 2015년 연구는 특히 긴 문장에서 이 고정 길이 병목이 성능을 제한한다고 보고 어텐션을 제안했다. 이 영어→프랑스어 중심 실험만으로 모든 언어쌍·자료 규모·문장 길이에서의 일반 성능을 확정하지 않는다.

Sennrich 등의 2016년 BPE NMT는 제한 word vocabulary를 문자·문자열 subword sequence로 바꿔 희귀 복합어·이름을 모델 안에서 생성하게 했다. 서브워드 토큰화는 고정 벡터 병목을 해결한 어텐션과 다른 축이며, 어휘를 줄이는 대신 sequence를 늘리는 비용이 있다.

## 학습 확인

1. NMT는 원문을 조건으로 어떤 확률 분포를 학습하는 기계 번역 접근인가?
2. 고정 벡터 seq2seq와 내용 기반 어텐션은 원문 정보를 디코더에 전달하는 방식이 어떻게 다른가?
3. IBM 정렬과 신경망 어텐션을 같은 연산이나 직접 역사 계보로 볼 수 없는 이유는 무엇인가?

다음에는 [[045_Sequence-to-Sequence 학습과 신경 기계 번역]]에서 초기 seq2seq 실험의 조건을 확인한다. 순환을 제거한 후속 번역 구조는 [[055_Transformer와 자기어텐션 기반 시퀀스 모델링]]에서 이어 본다.

## 출처

- [[045_Sequence-to-Sequence 학습과 신경 기계 번역]]
- [[053_GNMT와 제품 규모 신경 번역]]
- [[055_Transformer와 자기어텐션 기반 시퀀스 모델링]]
- [[022_IBM 통계적 기계 번역과 데이터 기반 전환]]
- Ilya Sutskever·Oriol Vinyals·Quoc V. Le, [Sequence to Sequence Learning with Neural Networks](https://proceedings.neurips.cc/paper_files/paper/2014/hash/5a18e133cbf9f257297f410bb7eca942-Abstract.html), 2014, pp. 3104–3112.
- Dzmitry Bahdanau·Kyunghyun Cho·Yoshua Bengio, [Neural Machine Translation by Jointly Learning to Align and Translate](https://arxiv.org/abs/1409.0473), 2015, §§1, 3.1, 5.2.1과 6.
- Ashish Vaswani 외, [Attention Is All You Need](https://proceedings.neurips.cc/paper_files/paper/2017/hash/3f5ee243547dee91fbd053c1c4a845aa-Abstract.html), 2017, pp. 5998–6008.
- Rico Sennrich·Barry Haddow·Alexandra Birch, [Neural Machine Translation of Rare Words with Subword Units](https://aclanthology.org/P16-1162/), ACL 2016, pp. 1715–1725.
- Yonghui Wu 외, [Google's Neural Machine Translation System](https://arxiv.org/abs/1609.08144), 2016, 특히 §§2–5.
- Quoc V. Le·Mike Schuster, [A Neural Network for Machine Translation, at Production Scale](https://research.google/blog/a-neural-network-for-machine-translation-at-production-scale/), Google Research, 2016-09-27.
- Ashish Vaswani 외, [Attention Is All You Need](https://proceedings.neurips.cc/paper_files/paper/2017/hash/3f5ee243547dee91fbd053c1c4a845aa-Abstract.html), NeurIPS 2017, pp. 5998–6008.

## 관련 항목

- [[045_Sequence-to-Sequence 학습과 신경 기계 번역]]
- [[053_GNMT와 제품 규모 신경 번역]]
- [[055_Transformer와 자기어텐션 기반 시퀀스 모델링]]
- [[Sequence-to-Sequence 학습]]
- [[인코더-디코더]]
- [[자기회귀 생성]]
- [[022_IBM 통계적 기계 번역과 데이터 기반 전환]]
- [[기계 번역]]
- [[규칙 기반 기계 번역]]
- [[통계적 기계 번역]]
- [[지도 학습]]
- [[026_순환 신경망과 시간적 문맥 학습]]
- [[순환 신경망]]
- [[장단기 메모리]]
