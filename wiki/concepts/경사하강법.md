---
schema_version: 2
id: concept.경사하강법
page_type: concept
title: 경사하강법
aliases:
  - Gradient Descent
  - gradient-based learning
  - 그래디언트 기반 학습
tags:
  - type/concept
  - domain/ai
  - domain/machine-learning
  - domain/mathematics
  - domain/optimization
created: '2026-07-14'
updated: '2026-07-24'
lifecycle: active
verification: verified
artifacts:
  - raw/006_1962_위드로-호프_MADALINE.md
  - raw/006_1962_위드로-호프_MADALINE_해설.md
  - raw/018_Backpropagation - Training Deep Neural Networks.ko.md
  - raw/018_Backpropagation - Training Deep Neural Networks.commentary.ko.md
  - 'raw/044_GloVe and Adam Optimizer Global Word Embeddings and Adaptive Optimization.ko.md'
  - 'raw/044_GloVe and Adam Optimizer Global Word Embeddings and Adaptive Optimization.commentary.ko.md'
evidence:
  - source_id: widrow-lehr-1990
    locator: pp. 1415–1433
    relation: supports
  - source_id: widrow-hoff-1960
    locator: Adaptive Switching Circuits
    relation: supports
  - source_id: rumelhart-hinton-williams-1986-pdp
    locator: pp. 322–328
    relation: supports
  - source_id: kingma-ba-2015-adam
    locator: 'arXiv:1412.6980, Algorithm 1과 §§2–2.1의 Adam 갱신·편향 보정'
    relation: supplements
  - source_id: reddi-kale-kumar-2018-adam-convergence
    locator: 'ICLR 2018, §§1–4의 Adam 수렴 반례와 AMSGrad'
    relation: contextualizes
related:
  - concept.미분-편미분-그래디언트
  - concept.lms-알고리즘
  - concept.adaline
  - concept.madaline
  - concept.지도-학습
  - concept.대규모-언어-모델
  - concept.역전파
  - concept.다층-퍼셉트론
  - concept.adam-최적화기
  - source.044
---
# 경사하강법

> [!note] 학습 안내
> **난이도:** 중급<br>
> **선수 지식:** [[미분·편미분·그래디언트]], [[지도 학습]]<br>
> **읽고 나면:** 손실의 그래디언트와 학습률이 한 번의 매개변수 갱신을 어떻게 만들고, 작은 예에서 갱신 전후 손실을 계산할 수 있다.

## 1단계 — 먼저 잡을 핵심

### 핵심 정의

[[경사하강법]](Gradient Descent)은 미분 가능한 목적 함수가 감소하는 방향으로 모델의 매개변수를 반복 조정하는 최적화 방법이다. 모든 학습 가능한 수를 $\theta$로 모으고 손실을 $J(\theta)$로 쓰면, 가장 단순한 한 걸음은 다음과 같다.

$$
\theta_{\mathrm{new}}
=
\theta-\eta\nabla_\theta J(\theta),
\qquad \eta>0
$$

$\nabla_\theta J$는 $\theta$와 같은 shape의 변화율 묶음이고, $\eta$는 사람이 정하는 양의 학습률이다. 그래디언트가 현재 지점에서 가장 빠른 증가 방향을 가리키므로, 고정된 작은 유클리드 길이 안에서 반대 부호가 1차 근사상 가장 빠른 감소 방향이 된다. 이 국소 해석과 도함수의 정의는 [[미분·편미분·그래디언트]]에서 완전하게 다룬다.

[[LMS 알고리즘]]은 순간 제곱오차에서 얻은 그래디언트 추정치로 [[ADALINE]]의 선형 가중치를 갱신한다. 그러나 한 예의 기울기를 쓰는 LMS·SGD, batch 전체 평균을 쓰는 batch gradient descent, 좌표별 상태를 더 쓰는 Adam은 같은 이름의 한 규칙이 아니다.

## 2단계 — 작동 원리

### 학습률

가중치를 한 번에 얼마나 바꿀지는 학습률이 제어한다. 값이 너무 크면 곡률이 큰 방향에서 손실이 오히려 커지거나 최솟값 주변을 넘나들 수 있고, 너무 작으면 같은 계산 예산 안에서 거의 움직이지 않는다. $\eta$는 그래디언트의 단위를 자동으로 없애는 보편 상수가 아니며, 손실의 scale·매개변수화·batch 구성과 함께 선택한다.

기본 확률적 경사하강법은 모든 좌표에 같은 scalar learning rate를 곱하지만 좌표별 gradient가 다르므로 실제 갱신량은 같지 않다. [[Adam 최적화기]]는 최근 gradient와 squared gradient의 지수평균으로 좌표별 갱신을 추가로 스케일한다. 기본값이 널리 쓰여도 모든 목적에서 수렴·속도·일반화를 보장하지 않는다.

### 한 스칼라 갱신을 끝까지 계산하기

설명용 손실을 $J(w)=(2w-1)^2$로 두자. $w=1.5$일 때 $J=4$이고, 미분하면

$$
\frac{dJ}{dw}=4(2w-1)=8
$$

학습률을 $\eta=0.1$로 정하면

$$
w_{\mathrm{new}}
=1.5-0.1\cdot8
=0.7
$$

갱신 뒤 손실은 $J(0.7)=(1.4-1)^2=0.16$이다. 이 한 걸음에서는 손실이 줄었지만, 모든 $w$·$\eta$·손실에서 항상 줄어든다는 증명은 아니다. 중심 차분으로 도함수를 확인하면

$$
\frac{J(1.501)-J(1.499)}{0.002}
=
\frac{4.008004-3.992004}{0.002}
=8
$$

이어서 해석적 기울기와 일치한다. 유한 차분은 작은 설명용 모형의 점검 도구이며, 수십억 매개변수의 실제 학습에서 매번 기울기를 구하는 방법은 아니다.

## 3단계 — 기술과 근거

### 신경망 계보

LMS는 선형 유닛에 적용되는 확률적 그래디언트 계열 학습이다. [[역전파]]는 [[다층 퍼셉트론]] 같은 합성 함수의 미분을 효율적으로 계산하는 방법이고, 경사하강법은 그 그래디언트로 매개변수를 갱신하는 최적화 절차다. 순전파·손실 계산·역전파·매개변수 갱신을 하나의 훈련 루프로 묶을 수 있지만 각 단계의 역할은 구분한다.

batch gradient descent는 자료 집합의 평균 손실 그래디언트를 계산한 뒤 한 번 갱신한다. SGD는 한 예 또는 작은 minibatch의 추정 기울기로 더 자주 갱신해 계산 비용·잡음·병렬화 조건을 다르게 만든다. Adam은 같은 gradient 입력에서 모멘트 상태와 좌표별 scaling을 추가한다. 어느 방법이 더 낫다는 결론은 목적 함수·자료·schedule·하드웨어와 평가 기준 없이는 낼 수 없다. [[퍼셉트론]] 갱신은 오분류에 반응하는 오류 수정 규칙이므로 일반적인 미분 가능 손실의 경사하강과도 같지 않다.

## 검증과 한계

### 적용 범위와 남은 한계

경사하강법은 미분 가능한 목적 함수와 계산된 그래디언트를 전제로 한다. 손실 표면은 비볼록일 수 있고, 그래디언트가 0인 지점이 전역 최솟값일 필요는 없으며, minibatch 기울기는 전체 자료의 기울기와 다를 수 있다. 학습률이나 좌표별 조정 방식을 선택했다는 사실만으로 수렴 속도와 일반화 성능이 보장되지는 않는다.

## 학습 확인

### 마스터리 연습

#### 완전 풀이 확인

본문의 $J(w)=(2w-1)^2$ 예를 미분, 중심 차분, update, 갱신 후 손실의 순서로 다시 계산하라. 역전파가 계산하는 값과 optimizer가 적용하는 값을 각각 표시한다.

#### 부분 완성

새 손실을 $J(w)=(w-3)^2$, 현재값을 $w=1$, 학습률을 $\eta=0.25$라고 하자. 다음 빈칸을 채워라.

$$
\frac{dJ}{dw}=\square,
\qquad
w_{\mathrm{new}}=\square,
\qquad
J(w_{\mathrm{new}})=\square
$$

gradient가 음수인데 $w$가 커지는 이유를 update 식의 두 부호로 설명한다.

#### 새 수치 전이

같은 $J(w)=(w-3)^2$, $w=1$에서 학습률만 $\eta=1.2$로 바꾼다. 새 $w$와 손실을 계산하고, 올바른 gradient를 사용했는데도 손실이 커진 이유를 설명하라.

#### 오류 진단

어떤 구현이 gradient $-4$를 얻고도 $w_{\mathrm{new}}=w+\eta\nabla J$를 사용했다. $\eta=0.25$일 때 결과와 손실을 계산해 잘못된 부호가 어떤 방향으로 움직였는지 찾는다. 이어서 “역전파가 학습률을 정한다”라는 문장도 고쳐 써라.

### 해설과 채점 기준

1. **부분 완성:** $dJ/dw=2(w-3)=-4$, $w_{\mathrm{new}}=1-0.25(-4)=2$, $J(2)=1$이다. 음수 gradient를 빼므로 매개변수는 양의 방향으로 움직인다.
2. **새 수치 전이:** $w_{\mathrm{new}}=1-1.2(-4)=5.8$, $J(5.8)=2.8^2=7.84$다. gradient는 현재 지점의 국소 방향만 주며, 너무 큰 학습률은 최솟값을 지나쳐 1차 근사가 믿을 수 있는 범위를 벗어난다.
3. **오류 진단:** 잘못된 더하기 식은 $w_{\mathrm{new}}=0$, $J(0)=9$를 만든다. 역전파는 gradient를 계산하고, 경사하강법이나 Adam 같은 optimizer가 학습률과 상태를 사용해 update를 정한다.

각 문제는 0–3점이다. 계산과 역할 구분이 모두 맞으면 3점, 산술 오류 하나만 있으면 2점, 마이너스 부호의 의미만 설명하면 1점이다. 총 7점 이상이면서 **gradient와 update를 같은 값으로 부르거나 부호를 뒤집는 오류가 없어야** 통과다. 통과하지 못하면 `핵심 정의`와 `한 스칼라 갱신을 끝까지 계산하기`를 복습한 뒤 $J(w)=(w+2)^2$, $w=0$, $\eta=0.1$로 재시도한다.

### 다음 문서

- [[Adam 최적화기]] — 일반적인 그래디언트 갱신에서 모멘트와 적응형 크기 조절을 결합한 후대 최적화기로 이어간다.
- [[역전파]] — 다층 합성 함수의 그래디언트를 효율적으로 계산하는 방법을 이어서 본다.

## 출처

- [[006_위드로-호프의 MADALINE]]
- [[018_역전파와 다층 신경망 학습]]
- [[044_GloVe와 Adam의 서로 다른 2014년 전환]]
- Bernard Widrow·Michael A. Lehr, [30 Years of Adaptive Neural Networks](https://isl.stanford.edu/people/widrow/papers/j199030years.pdf), 1990, pp. 1428–1432.
- David E. Rumelhart·Geoffrey E. Hinton·Ronald J. Williams, [Learning Internal Representations by Error Propagation](https://doi.org/10.7551/mitpress/5236.003.0012), 1986, pp. 322–328.
- Diederik P. Kingma·Jimmy Ba, [Adam: A Method for Stochastic Optimization](https://arxiv.org/abs/1412.6980), 2014년 12월 공개·ICLR 2015.
- Sashank J. Reddi·Satyen Kale·Sanjiv Kumar, [On the Convergence of Adam and Beyond](https://openreview.net/forum?id=ryQu7f-RZ), ICLR 2018.

## 관련 항목

- [[미분·편미분·그래디언트]]
- [[LMS 알고리즘]]
- [[ADALINE]]
- [[MADALINE]]
- [[지도 학습]]
- [[대규모 언어 모델]]
- [[역전파]]
- [[다층 퍼셉트론]]
- [[Adam 최적화기]]
- [[044_GloVe와 Adam의 서로 다른 2014년 전환]]
