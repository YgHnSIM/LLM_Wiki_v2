---
schema_version: 2
id: concept.신경-정보-검색
page_type: concept
title: 신경 정보 검색
aliases:
  - neural information retrieval
  - neural IR
  - 신경 검색
tags:
  - type/concept
  - domain/ai
  - domain/computer-science
  - domain/nlp
  - domain/machine-learning
created: '2026-07-19'
updated: '2026-07-21'
lifecycle: active
verification: verified
artifacts:
  - 'raw/051_Neural Information Retrieval Semantic Search with Deep Learning.ko.md'
  - 'raw/051_Neural Information Retrieval Semantic Search with Deep Learning.commentary.ko.md'
  - 'raw/064_BERT for Information Retrieval Transformer-Based Ranking and Semantic Search.ko.md'
  - 'raw/064_BERT for Information Retrieval Transformer-Based Ranking and Semantic Search.commentary.ko.md'
evidence:
  - source_id: huang-et-al-2013-dssm
    locator: '§§1·3–4의 공통 저차원 표현·cosine score·clickthrough conditional likelihood'
    relation: supports
  - source_id: shen-et-al-2014-cdssm
    locator: '본문의 convolution-max pooling 지역 문맥 표현과 Web ranking 평가'
    relation: supports
  - source_id: guo-et-al-2016-drmm
    locator: '§§1·3–5의 representation/interaction 모델 구분과 relevance matching 요소·reranking 평가'
    relation: supports
  - source_id: karpukhin-et-al-2020-dpr
    locator: '§§2–3의 dual-encoder dense passage retrieval과 negative 설계'
    relation: contextualizes
  - source_id: nogueira-cho-2019-bert-reranking
    locator: '§2와 Eq. 1의 BERT query–passage 이진 분류·BM25 상위 1,000개 재순위화·512-token 결합 입력, §3과 Table 1의 MS MARCO·TREC-CAR 평가'
    relation: supports
  - source_id: macavaney-et-al-2019-cedr
    locator: '§§1–3의 BERT classification vector·문맥화 token 표현과 PACRR·KNRM·DRMM 결합, 최대 입력 길이와 실행 비용 분석'
    relation: contextualizes
related:
  - source.051
  - source.064
  - concept.bm25
  - concept.벡터-공간-모델
  - concept.교차-인코더-재순위화
---
# 신경 정보 검색

> [!note] 학습 안내
> **난이도:** 중급<br>
> **선수 지식:** [[벡터 공간 모델]]<br>
> **읽고 나면:** 신경 정보 검색이 표현·상호작용·순위 가운데 무엇을 학습하는지와 첫 단계 검색·재순위화의 차이를 설명할 수 있다.

## 1단계 — 먼저 잡을 핵심

신경 정보 검색(neural information retrieval)은 질의와 문서의 표현, 두 입력 사이의 대응 패턴, 또는 최종 순위 점수 가운데 하나 이상을 신경망으로 학습하는 검색 접근군이다. 하나의 “의미 검색 알고리즘”이 아니라, 전역 벡터 비교부터 token interaction 재순위화와 현대 dense passage retrieval까지 서로 다른 구조를 포함한다.

## 2단계 — 작동 원리

### 무엇을 학습하는가

| 층위 | 학습 대상 | 예 |
| --- | --- | --- |
| 표현 | 질의·문서를 공통 벡터 공간에 사상 | DSSM·C-DSSM·DPR |
| 상호작용 | 질의어와 문서어의 국소 대응 패턴 | DRMM |
| 순위 | 양성 문서가 음성 문서보다 높도록 점수 조정 | click likelihood·pairwise loss |
| 교차 인코더 재순위화 | 결합한 질의–후보의 문맥 표현과 관련성 점수 | BERT reranker |

`end-to-end`라는 말도 범위를 밝혀야 한다. DSSM은 입력 표현부터 클릭 가능도까지 학습하지만 클릭 자료의 수집, 음성 표본, 후보 문서는 시스템 밖에서 주어진다. DRMM은 사전학습 Word2Vec term vector를 고정하고 matching network를 학습했다. 모든 구성 요소를 공동 학습했다는 뜻은 아니다.

### 표현 중심과 상호작용 중심

표현 중심 모델은 질의와 문서를 각각 한 벡터로 만든 뒤 코사인 유사도나 내적으로 비교한다. 문서 벡터를 미리 계산할 수 있어 큰 후보 집합에 유리하지만, 긴 문서의 특정 위치·exact match·부정 같은 세부 관계가 전역 압축에서 약해질 수 있다.

상호작용 중심 모델은 질의와 문서 token 사이의 국소 점수를 먼저 만든다. 더 세밀한 relevance를 볼 수 있지만 질의가 들어온 뒤 문서별 계산이 필요해 보통 제한된 후보 재순위화에 적합하다. 현대의 dual encoder와 [[교차 인코더 재순위화|cross-encoder]] 구분도 이 계산 trade-off를 이어 간다. BERT cross-encoder는 질의와 후보를 하나의 시퀀스로 함께 문맥화하므로 문서 표현을 질의와 독립적으로 사전 계산할 수 없다.

## 3단계 — 기술과 근거

### semantic matching과 relevance matching

문장 두 개가 같은 뜻인지 판단하는 semantic matching과, 문서가 질의의 정보 요구를 충족하는지 판단하는 relevance matching은 다르다. 긴 문서는 질의와 전체 의미가 비슷하지 않아도 한 passage에 답이 있을 수 있다. 희귀 질의어의 exact match가 관련성에 결정적일 수도 있다.

따라서 신경 검색은 [[BM25]] 같은 희소 lexical 신호를 자동으로 폐기하는 세대 교체가 아니다. 희소 검색으로 후보를 만들고 신경망으로 재순위화하거나, 희소·밀집 후보를 결합할 수 있다.

### 학습 자료와 음성 표본

클릭 기록은 대규모 감독을 제공하지만 위치·노출·브랜드·snippet 편향이 있다. 비클릭 문서가 반드시 무관한 것도 아니다. 무작위 negative는 너무 쉬울 수 있고 hard negative는 유용하지만 false negative를 포함할 수 있다. 어떤 음성을 사용했는지가 검색 경계의 일부다.

### 검색 파이프라인에서의 위치

신경 모델의 역할을 말할 때 후보 생성과 재순위화를 구분해야 한다.

1. **첫 단계 검색**은 전체 컬렉션에서 높은 재현율로 후보를 만든다.
2. **재순위화**는 상위 후보에 더 비싼 질의-문서 상호작용을 적용한다.
3. **reader·generator**는 검색 근거에서 답을 추출하거나 생성한다.

DRMM은 전통 언어 모형이 만든 2,000개 후보를 재순위화했다. DPR은 passage encoder와 벡터 검색으로 첫 단계 retrieval을 수행했다. 두 결과를 모두 “neural retrieval”이라고 부를 수 있지만 대기 시간·실패 원인·평가 지표가 다르다.

[[064_BERT 기반 passage 재순위화]]의 대표 실험은 BM25가 가져온 상위 1,000개 passage 각각에 BERT를 적용했다. `[CLS]` 표현의 이진 분류 점수로 후보를 다시 정렬했으며, 학습은 후보 쌍을 독립적으로 판정하는 pointwise cross-entropy를 사용했다. 따라서 BERT의 강한 문맥 표현이 전체 컬렉션의 후보 생성 비용을 없앤 것이 아니다. 첫 단계가 관련 passage를 1,000개 안에 넣지 못하면 재순위화도 복구할 수 없다.

같은 시기 CEDR는 BERT의 classification vector와 문맥화된 token 표현을 기존 PACRR·KNRM·DRMM 랭커에 결합했다. 이는 BERT 기반 IR이 하나의 `[CLS]` cross-encoder 형태로만 수렴한 것이 아니라, 사전학습 표현과 기존 interaction architecture를 연결하는 경로도 함께 탐색됐음을 보여 준다.

### 현대 RAG와의 연결

RAG의 dense retriever는 질의·passage 독립 인코딩과 유사도 검색을 사용한다는 점에서 DSSM 계열과 구조적 공통점이 있다. 그러나 Transformer 사전학습, passage 자료, in-batch·hard negatives, ANN 색인, 검색 결과를 조건으로 한 생성이 추가된다. 초기 신경 검색과 현대 RAG 사이에는 연속성도 있고 중요한 기술적 단절도 있다.

## 검증과 한계

신경 검색은 희소 lexical 검색의 단순한 대체 세대가 아니다. 클릭 자료와 negative sampling의 편향, 후보 생성과 재순위화의 다른 비용, exact match 손실 가능성을 함께 평가해야 하며 초기 DSSM·DRMM에서 현대 DPR·RAG까지를 하나의 직접 계보로 축약하지 않는다.

## 학습 확인

### 확인 질문

1. 신경 정보 검색은 표현·상호작용·순위 가운데 무엇을 학습할 수 있는가?
2. 표현 중심과 상호작용 중심 모델은 사전 계산과 세부 대응에서 어떤 trade-off를 가지는가?
3. DRMM과 DPR을 모두 neural retrieval이라 부르더라도 파이프라인 역할을 구분해야 하는 이유는 무엇인가?

### 다음 문서

- [[050_SQuAD와 추출형 독해 평가]] — 검색된 근거 안에서 답 구간을 고르는 독해 단계와 검색을 대조한다.

## 출처

- [[051_신경 정보 검색과 의미 대응]]
- [[064_BERT 기반 passage 재순위화]]
- Po-Sen Huang 외, [Learning Deep Structured Semantic Models for Web Search using Clickthrough Data](https://doi.org/10.1145/2505515.2505665), CIKM 2013, pp. 2333–2338.
- Yelong Shen 외, [Learning Semantic Representations Using Convolutional Neural Networks for Web Search](https://www.microsoft.com/en-us/research/wp-content/uploads/2014/04/www2014_cdssm_p06.pdf), WWW Companion 2014.
- Jiafeng Guo 외, [A Deep Relevance Matching Model for Ad-hoc Retrieval](https://doi.org/10.1145/2983323.2983769), CIKM 2016, pp. 55–64.
- Vladimir Karpukhin 외, [Dense Passage Retrieval for Open-Domain Question Answering](https://aclanthology.org/2020.emnlp-main.550/), EMNLP 2020.
- Rodrigo Nogueira·Kyunghyun Cho, [Passage Re-ranking with BERT](https://arxiv.org/abs/1901.04085), 2019.
- Sean MacAvaney 외, [CEDR: Contextualized Embeddings for Document Ranking](https://arxiv.org/abs/1904.07094), SIGIR 2019.
- 프로젝트 보존 자료: `raw/051_Neural Information Retrieval Semantic Search with Deep Learning.ko.md`와 대응 해설.
- 프로젝트 보존 자료: `raw/064_BERT for Information Retrieval Transformer-Based Ranking and Semantic Search.ko.md`와 대응 해설.

## 관련 항목

- [[051_신경 정보 검색과 의미 대응]]
- [[064_BERT 기반 passage 재순위화]]
- [[BM25]]
- [[벡터 공간 모델]]
- [[교차 인코더 재순위화]]
