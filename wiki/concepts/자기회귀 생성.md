---
schema_version: 2
id: concept.자기회귀-생성
page_type: concept
title: 자기회귀 생성
aliases: [Autoregressive Generation, 오토리그레시브 생성]
tags:
  - type/concept
  - domain/ai
  - domain/nlp
  - domain/machine-learning
created: '2026-07-18'
updated: '2026-07-18'
lifecycle: active
verification: verified
artifacts:
  - 'raw/045_Sequence-to-Sequence Neural Machine Translation End-to-End Learning Revolution.ko.md'
  - 'raw/045_Sequence-to-Sequence Neural Machine Translation End-to-End Learning Revolution.commentary.ko.md'
evidence:
  - source_id: sutskever-vinyals-le-2014-seq2seq
    locator: '§2, equations (1)–(2)와 Figure 1, pp. 3105–3106'
    relation: supports
  - source_id: bengio-et-al-2003-nplm
    locator: '§2.1의 joint probability 연쇄 분해와 다음 단어 조건부 분포, pp. 1138–1141'
    relation: contextualizes
related:
  - source.045
  - concept.sequence-to-sequence
  - concept.인코더-디코더
  - concept.신경-확률-언어-모형
---
# 자기회귀 생성

[[자기회귀 생성]]은 앞서 생성한 요소에 조건화해 다음 요소를 하나씩 뽑아 시퀀스를 만드는 방식이다. 언어에서는 (p(y_t\mid y_{<t},x))를 반복 계산하며, (x)가 없는 언어 모델과 원문 (x)가 있는 번역 디코더를 모두 이 틀로 표현할 수 있다.

## 학습과 생성의 차이

정답 시퀀스가 있는 최대 가능도 학습에서는 이전 정답 토큰을 입력해 각 다음 토큰 손실을 계산할 수 있다. 이를 순환 모델 문맥에서 teacher forcing이라 부른다. 추론 때는 정답을 모르므로 모델이 고른 토큰을 다시 입력한다. 이 조건 차이 때문에 초반 오류가 뒤 예측에 영향을 줄 수 있다.

RNN에서는 teacher forcing을 사용해도 현재 은닉 상태가 이전 은닉 상태에 의존하므로 시간축 계산이 순차적이다. Transformer는 훈련 때 causal mask 아래 여러 위치를 병렬 계산할 수 있지만, 표준 자기회귀 추론은 이미 생성한 토큰을 필요로 하므로 여전히 순차적이다.

## 디코딩은 확률모형과 다르다

모델이 다음 토큰 분포를 계산하는 것과 최종 문장을 선택하는 탐색 절차는 구분해야 한다. greedy decoding은 각 단계 최댓값을 택하고, beam search는 여러 부분 가설을 유지하며, sampling은 분포에서 표본을 뽑는다. 같은 모델도 디코딩 규칙에 따라 길이·다양성·품질이 달라질 수 있다.

자기회귀 언어 모델은 2014년 seq2seq보다 오래됐다. seq2seq 디코더가 번역과 일반 시퀀스 변환에서 이 방식을 널리 사용했지만 자기회귀 생성 자체를 처음 발명한 것은 아니다.

## 출처

- [[045_Sequence-to-Sequence 학습과 신경 기계 번역]]
- Ilya Sutskever·Oriol Vinyals·Quoc V. Le, [Sequence to Sequence Learning with Neural Networks](https://proceedings.neurips.cc/paper_files/paper/2014/hash/5a18e133cbf9f257297f410bb7eca942-Abstract.html), 2014, §2.
- Yoshua Bengio 외, [A Neural Probabilistic Language Model](https://www.jmlr.org/papers/v3/bengio03a.html), 2003, §2.1.

## 관련 항목

- [[045_Sequence-to-Sequence 학습과 신경 기계 번역]]
- [[Sequence-to-Sequence 학습]]
- [[인코더-디코더]]
- [[신경 확률 언어 모형]]
