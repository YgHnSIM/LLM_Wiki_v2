---
schema_version: 2
id: analysis.공개-가중치와-재현-가능성은-같은-축인가
page_type: analysis
title: 공개 가중치와 재현 가능성은 같은 축인가
aliases:
  - open weights와 reproducibility
  - 공개 접근과 재현 가능성
  - 오픈 모델의 재현 장부
  - 모델 공개 층위와 연구 재현
tags:
  - type/analysis
  - domain/ai
  - domain/computer-science
  - domain/nlp
  - domain/machine-learning
  - domain/academia
created: '2026-07-21'
updated: '2026-07-21'
lifecycle: active
verification: partial
artifacts:
  - 'raw/074_The Pile Open-Source Training Dataset for Large Language Models.ko.md'
  - 'raw/074_The Pile Open-Source Training Dataset for Large Language Models.commentary.ko.md'
  - 'raw/076_Foundation Models Report Defining a New Paradigm in AI.ko.md'
  - 'raw/076_Foundation Models Report Defining a New Paradigm in AI.commentary.ko.md'
  - 'raw/079_HELM Holistic Evaluation of Language Models Framework.ko.md'
  - 'raw/079_HELM Holistic Evaluation of Language Models Framework.commentary.ko.md'
  - 'raw/082_BLOOM Open-Access Multilingual Language Model and the Democratization of AI Research.ko.md'
  - 'raw/082_BLOOM Open-Access Multilingual Language Model and the Democratization of AI Research.commentary.ko.md'
evidence:
  - source_id: gao-et-al-2020-pile
    locator: '§§1.1·3–4·6–7, Tables 1·5와 Appendices C–D의 22개 구성요소·배포·처리 code·공개 접근성·host terms·저자 동의·법적 한계'
    relation: supports
  - source_id: biderman-et-al-2022-pile-datasheet
    locator: 'PDF pp. 1·8–17의 dataset 배포, MIT project license 표기, 구성요소별 저작권·개인정보·동의·검증 한계'
    relation: supplements
  - source_id: bommasani-et-al-2021-foundation-models
    locator: '§§1.3–1.4, §4.4 (pp. 91–96), §§5.1·5.3·5.6 (pp. 129–160)의 model 접근, 적응 data·compute 통제, 환경 비용, 산업 자원 집중과 학계 접근성'
    relation: supports
  - source_id: bigscience-workshop-2022-bloom
    locator: '§§2.2·3.1–3.5·4.2–4.9·5와 Tables 1·3–4의 협업 구조, ROOTS, 공개 artifact, Apache 2.0 code, 384 A100·1,082,990 GPU-hours와 평가 한계'
    relation: supports
  - source_id: laurencon-et-al-2023-roots
    locator: '초록과 §§1–5, Appendix A의 498개 구성 dataset·1.6TB corpus, gated initial release, 출처별 license·release 전략, data governance와 대표성 한계'
    relation: supports
  - source_id: bigscience-2022-bloom-rail
    locator: 'Preamble, §I.2·6, §§II–III와 Attachment A의 Model·Data·Complementary Material 구분, 이용 제한과 재배포 고지 의무'
    relation: supports
  - source_id: liang-et-al-2023-helm
    locator: '§§1.1–1.2·3–5·8·10–11, Tables 4–6·8·13과 Appendices C·F–J의 scenario·adaptation·metric 명세, prompt·completion 공개, 실행 비용, API version 변화와 재현 한계'
    relation: supports
related:
  - source.074
  - source.076
  - source.079
  - source.082
  - concept.the-pile
  - concept.파운데이션-모델
  - concept.helm
  - concept.bloom
  - analysis.데이터-품질과-분포-다양성은-같은-축인가
---
# 공개 가중치와 재현 가능성은 같은 축인가

> [!note] 학습 안내
> **난이도:** 심화<br>
> **선수 지식:** [[파운데이션 모델]], [[The Pile]], [[HELM]], [[BLOOM]]<br>
> **읽고 나면:** 문서·data·code·model weight·평가 출력의 공개 범위를 나누고, 검사·변형·재배포·재실행·재학습·의사 결정 참여가 왜 서로 다른 접근 축인지 설명할 수 있다.

## 1단계 — 먼저 잡을 핵심

### 비교 질문

[[074_The Pile과 대규모 언어 모델 학습 말뭉치|The Pile]]은 대규모 학습 자료와 구성 code를 공개했지만 project-level license가 원문 각각의 권리 상태를 하나로 만들지는 않았다. [[076_파운데이션 모델 보고서와 AI 생태계|파운데이션 모델 보고서]]는 이미 훈련된 model의 재사용이 후속 개발 비용을 낮출 수 있는 동시에 model 접근, 적응 compute와 engineering expertise가 새 장벽이 될 수 있다고 지적했다. [[082_BLOOM과 공개 접근 다국어 LLM|BLOOM]]은 문서·weight·Apache 2.0 code·ROOTS 일부 접근을 결합했지만 각 artifact의 조건과 실행 비용은 달랐다. [[079_HELM과 다차원 언어 모델 평가|HELM]]은 scenario와 prompt·completion을 남겨 결과 감사를 돕지만, 평가한 폐쇄형 API의 내부 weight나 미래 version을 고정하지 못한다.

이 네 사례를 함께 읽으면 답은 명확하다. **공개 가중치는 검사 가능성과 변형 가능성을 크게 넓히지만, 재현 가능성과 같은 축은 아니다.** 재현하려는 대상이 학습인지, 특정 checkpoint의 추론인지, 한 평가 run인지에 따라 필요한 data·code·환경·계산·version 기록이 달라진다.

### ‘열려 있다’보다 먼저 목적어와 동사를 적는다

`오픈`이라는 한 단어에는 서로 다른 질문이 겹친다.

| 목적어 | 물어야 할 동사 | 확인할 조건 |
| --- | --- | --- |
| 문서·model card | 읽고 검토할 수 있는가 | 공개 범위, version, 누락된 설계 판단 |
| Training data | 조사·재구성·재배포할 수 있는가 | 원천별 권리, consent, 접근 심사, snapshot |
| Code·환경 | 실행하고 수정할 수 있는가 | license, dependency·hardware version, 설정·seed |
| Model weight | 내려받고 검사·변형·재배포할 수 있는가 | 이용 제한, 파생물 조건, 저장·memory 요구량 |
| 평가 산출물 | score에서 prompt·completion으로 내려갈 수 있는가 | scenario, adaptation, metric, query 시각·model version |
| 개발 과정 | 의제와 규칙을 함께 정할 수 있는가 | 참여 경로, 의사 결정권, compute 배분, 책임 구조 |

공개 URL이 있다는 사실은 이 동사 전부에 자동으로 `예`라고 답하지 않는다. 반대로 완전한 재학습이 어렵다는 이유만으로 공개 weight가 제공하는 독립적 probing·fine-tuning·오류 분석의 가치가 사라지는 것도 아니다.

## 2단계 — 작동 원리

### 재현 대상마다 필요한 교집합이 다르다

**추론 재실행**은 같은 checkpoint, tokenizer·inference code, decoding 설정, 입력과 계산 자원이 있어야 비교할 수 있다. **평가 재실행**은 여기에 scenario data, prompt 구성, adaptation 절차, metric 구현과 model version 기록이 더 필요하다. **학습 재현**은 training data snapshot과 순서·가중치, architecture, optimizer, hyperparameter, distributed system, random seed와 충분한 compute까지 요구한다.

이를 한 개의 점수 대신 조건 집합으로 쓰면 다음과 같다.

$$
R_t = D_t \cap C_t \cap E_t \cap H_t \cap B_t
$$

여기서 $R_t$는 대상 $t$의 재현 조건, $D_t$는 data, $C_t$는 code·configuration, $E_t$는 software·hardware environment, $H_t$는 실행 가능한 계산 자원, $B_t$는 weight·output·log 같은 기준 artifact다. 어느 한 항목의 공개는 다른 항목의 결손을 대신하지 않는다. 이 식은 재현 여부를 자동 판정하는 metric이 아니라, 빠진 조건을 기록하기 위한 장부다.

### 여섯 축 비교 장부

아래 표의 평가는 `좋음/나쁨` 순위가 아니라 각 1차 자료가 직접 확인하게 해 주는 범위다.

| 사례 | 검사 가능성 | 변형 가능성 | 재배포 가능성 | 재현 가능성 | 실행 비용 | 거버넌스 참여 |
| --- | --- | --- | --- | --- | --- | --- |
| The Pile | 22개 component, 처리와 위험 문서로 구성·분포를 조사할 수 있음 | 공개 구성 code와 data로 subset·mixture를 다시 만들 여지가 있음 | Datasheet는 MIT 배포를 기록하지만 underlying text의 저작권·ToS·동의는 component마다 다름 | 같은 corpus 변수를 맞추는 데 도움을 주지만 tokenizer·model·compute·seed까지 고정하지 않음 | 약 825.18 GiB raw data의 저장·처리와 model 학습 비용이 별도 | Curator 판단과 component 선택은 문서화되지만 원 저자·모든 영향 집단의 공동 결정은 입증되지 않음 |
| 파운데이션 모델 보고서 | Model 생태계의 access·adaptation·harm 층을 분석하는 개념 장부를 제공 | 이미 훈련된 model 접근은 후속 적응 가능성을 늘릴 수 있음 | 특정 artifact의 license를 정한 release 문서가 아니라 연구 의제 보고서임 | 공정한 비교에는 adaptation data·model access·compute 통제가 필요하다고 명시 | 사전학습 비용을 공유해도 적응 compute·expertise와 반복 inference 비용이 남음 | 하위 개발자 증가와 기반 model 설계 권력 분산을 구분하고 산업 자원 집중을 문제 삼음 |
| BLOOM·ROOTS·RAIL | 논문·개발 문서·checkpoint·data card로 architecture, corpus와 위험을 외부에서 조사할 수 있음 | Weight는 허용 범위에서 probing·fine-tuning·파생 model 연구가 가능하고 code는 Apache 2.0 | Weight·파생물은 RAIL의 이용 제한·고지 의무, code는 Apache 2.0, data는 RAIL 대상 밖이며 source별 조건 | Checkpoint 추론·변형 재실행은 쉬워지지만 exact retraining에는 release 가능한 ROOTS, 환경과 대규모 compute가 더 필요 | 최종 학습은 A100 80GB 384개와 1,082,990 GPU-hours를 사용해 법적 접근과 실제 실행 능력이 갈림 | 다수 working group과 data governance 절차가 참여 통로를 만들었지만 참여자 수가 동등한 결정권·자원 배분을 뜻하지 않음 |
| HELM | Scenario·adaptation·metric과 원시 prompt·completion으로 집계 score를 감사할 수 있음 | 공개 평가 구현과 scenario 조합은 수정·확장할 수 있으나 폐쇄형 API model 자체는 변형할 수 없음 | 평가 code·산출물 공개와 대상 model의 재배포 권리는 별개 | 같은 명세의 평가를 재실행할 수 있어도 변경 가능한 API의 과거 내부 model을 복원하지 못함 | 4,939개 run과 API·추론 비용이 들었으며 model access 조건에 따라 재실행 범위가 달라짐 | 빠진 scenario·metric을 드러내지만 어떤 가치·가중치를 우선할지는 별도 사회적 결정으로 남음 |

### 축 사이의 대표적인 비대칭

- Weight를 받을 수 있어도 training data를 같은 snapshot으로 얻지 못하면 **변형은 가능하지만 학습 재현은 불완전**할 수 있다.
- Data와 code를 받을 수 있어도 필요한 cluster가 없으면 **법적 접근은 있어도 실행 접근은 없을** 수 있다.
- Prompt와 completion을 볼 수 있어도 API 제공자가 내부 model을 바꾸면 **과거 결과는 감사할 수 있지만 같은 model의 재질의는 불가능**할 수 있다.
- 수천 명이 협업 공간에 참여해도 compute allocation과 최종 release rule을 누가 정했는지 모르면 **참여 규모와 거버넌스 권한을 같게 셀 수 없다.**

## 3단계 — 기술과 근거

### The Pile: project license와 원문 권리는 다른 층이다

The Pile 논문과 datasheet는 22개 component, 처리 방법, raw·effective size와 위험을 상세히 기록했다. Datasheet는 dataset을 MIT License로 배포했다고 설명한다. 동시에 Books3를 비롯한 구성요소의 저작권, host terms, PII와 저자 동의가 균일하지 않으며 규모 때문에 모든 document를 전수 확인할 수 없다고 적었다.

따라서 project-level license는 연구자가 배포자가 제시한 조건을 확인하게 하지만, 수집된 모든 underlying work의 권리를 새로 통일하는 장치로 읽을 수 없다. 이 구분은 재현성에도 직접 영향을 준다. 뒤에 원천 접근 조건이 바뀌거나 특정 component를 재배포할 수 없다면 같은 이름의 corpus를 받더라도 동일 snapshot을 재구성하기 어려울 수 있다.

The Pile을 공개했다는 사실은 corpus 구성과 data 변수를 더 잘 조사하게 한다. 그러나 같은 자료로 학습한다는 것만으로 model 재현이 끝나지 않는다. Tokenizer, component weight, document order, architecture, optimizer, seed와 compute가 달라지면 다른 실험이다.

### 파운데이션 모델 보고서: 접근 확대와 자원 집중은 공존한다

파운데이션 모델 보고서는 model을 처음부터 다시 훈련하지 않고 여러 과제에 적응시키는 구조가 비용을 나눌 기회를 만든다고 설명한다. 그러나 §4.4의 공정한 비교 조건에는 적응 data, model access와 compute가 함께 들어간다. §1.3과 §5.6은 대규모 사전학습 능력과 핵심 model 접근이 소수 산업 조직에 집중될 수 있다는 문제를 별도로 다룬다.

이는 `weight를 받았다`와 `연구 조건이 평등해졌다` 사이의 간격을 보여 준다. 공개 weight는 base model 개발자가 아니어도 내부 activation 조사나 fine-tuning을 시도하게 할 수 있다. 하지만 checkpoint를 저장·실행할 hardware, 적응 data와 engineering expertise가 집중돼 있다면 가능한 연구의 폭은 집단마다 다르다. 접근성은 artifact의 법적 상태뿐 아니라 **적응 가능한 자원**까지 포함해 평가해야 한다.

### BLOOM: 한 model 안에서도 공개 조건이 네 갈래로 나뉜다

BLOOM은 다음 층을 하나의 연구 프로젝트에서 함께 제시했다.

| 층 | 확인된 공개 범위 | 재현 장부에서 남는 조건 |
| --- | --- | --- |
| 문서 | Architecture, 학습·평가, model card 성격의 기록과 협업 과정 | 모든 시행착오·환경 상태가 한 문서에 완전히 보존됐는지 별도 확인 필요 |
| Weight | 완성 checkpoint와 일부 중간 artifact에 접근 가능 | RAIL의 이용·재배포 제한, 저장·memory·inference compute |
| Code | 학습·실행 code를 Apache 2.0으로 공개 | Dependency·driver·distributed runtime와 hardware 조건 |
| ROOTS data | 구성·data card와 gated인 큰 초기 subset, source별 release 전략 | 전체 text의 단일 자유 license가 없고 동일 snapshot 재구성에 권리·접근 차이 존재 |

RAIL은 `Data`를 license 대상에서 제외하고, Model과 파생물에는 Attachment A의 use-based restriction과 재배포 고지 의무를 둔다. 이용 제한은 아무 조건 없는 재배포와 다르지만, checkpoint의 검사·허용된 변형 가능성을 없애는 것도 아니다. `open weights`, `open-source code`, `open data`를 한 단어로 합치면 이 차이가 사라진다.

실행 장벽은 수치로도 드러난다. BLOOM 최종 학습은 Jean Zay의 A100 80GB GPU 384개에서 진행됐고 1,082,990 GPU-hours가 보고됐다. 공개 artifact는 exact retraining의 정보 장벽을 낮추지만 이 계산량을 모든 연구자에게 배분하지 않는다. 공공 HPC를 이용한 국제 협업은 기업 외부의 대규모 학습 가능성을 보여 준 사례이면서, 국가급 계산 allocation이라는 집중된 기반에도 의존한 사례다.

### ROOTS와 BigScience: data governance는 access control보다 넓다

ROOTS는 구성 data의 언어·출처와 처리 과정을 문서화하고, native speaker 참여와 data governance를 corpus 설계의 일부로 다뤘다. 큰 초기 공개분은 gated access와 윤리 헌장 동의를 요구했고, 원천별 license·release 조건을 따랐다. 이 장치는 무차별 download와 다른 책임 구조를 만든다.

그러나 gate가 있다는 사실만으로 원 저자 동의, 언어 공동체 대표성과 모든 downstream harm이 해결됐다고 결론 내릴 수 없다. 반대로 gate 때문에 corpus 문서화와 공동 심의의 가치가 사라지는 것도 아니다. 거버넌스는 `파일을 받을 수 있는가`뿐 아니라 **누가 포함·제외 기준과 release rule을 정하고, 이의를 제기하며, 자원을 배분하는가**를 묻는 별도 축이다.

### HELM: 출력 감사와 model 재현을 구분한다

HELM은 task·domain·language로 scenario를 정하고, adaptation과 metric을 표준화하며, score뿐 아니라 원시 prompt·completion을 공개했다. 이 구조는 결과표의 한 점에서 실제 입력·출력으로 내려가 오류와 측정 방식을 검사하게 한다. 같은 scenario와 metric을 공개 구현으로 재실행하거나 새로운 model에 적용할 통로도 제공한다.

하지만 HELM은 open model, 제한적 API와 closed-access model을 함께 평가했다. 비공개 API는 평가 기간에도 바뀔 수 있었고 제공자별 version 기록도 일관되지 않았다. Query 시각과 알려진 version을 남겨도, 제공자가 과거 내부 checkpoint를 공개하지 않으면 외부 연구자가 그 model 자체를 복원할 수 없다. **공개된 원시 출력은 과거 run의 감사 기록이지, mutable service의 실행 파일이 아니다.**

따라서 평가 재현은 적어도 두 수준으로 나눠야 한다.

1. **절차 재현:** 같은 scenario·prompt·metric code를 다시 실행한다.
2. **관측 재현:** 같은 model version과 조건에서 같은 분포의 응답을 얻는다.

HELM은 첫 번째 수준의 장벽을 크게 낮추고 과거 관측을 남겼다. 폐쇄형 API에 대해서는 두 번째 수준을 독립적으로 보장하지 못한다.

### 네 자료를 함께 읽을 때 얻는 실무 규칙

연구 artifact를 공개하거나 사용할 때는 최소한 다음 문장을 완성해야 한다.

- **검사:** 누구나 무엇의 어느 version까지 볼 수 있다.
- **변형:** 어떤 artifact를 어떤 조건에서 바꿔 파생물을 만들 수 있다.
- **재배포:** 원본과 파생물에 어떤 license·use restriction·source-specific right가 적용된다.
- **재현:** 추론·평가·학습 가운데 무엇을 어떤 tolerance로 반복하려는지 정한다.
- **실행:** 필요한 storage, memory, accelerator time과 전문성을 기록한다.
- **거버넌스:** 포함 기준, 접근 심사, model release와 compute 배분을 누가 결정했는지 기록한다.

이 여섯 문장을 채우면 `오픈인가 아닌가`라는 이분법보다 무엇이 실제로 가능하고 무엇이 빠졌는지 재검토하기 쉬워진다.

## 검증과 한계

### 확인된 사실

- The Pile 자료는 project-level MIT 배포 표기와 underlying component의 저작권·ToS·동의 차이를 함께 기록한다. 공개 corpus라는 이름만으로 모든 원문의 권리 상태가 같아지지 않는다.
- 파운데이션 모델 보고서는 model 재사용의 비용 분담 가능성과 model access·적응 compute·expertise의 집중 위험을 함께 제시한다.
- BLOOM의 weight, Apache 2.0 code와 ROOTS data는 서로 다른 조건으로 공개됐다. RAIL은 Data를 대상에서 제외하고 Model·파생물의 이용 제한과 재배포 고지 의무를 정한다.
- BLOOM 최종 학습에는 A100 80GB GPU 384개와 1,082,990 GPU-hours가 사용됐다. Artifact 공개가 이 계산 자원을 자동 배분하지 않는다.
- HELM은 scenario·adaptation·metric과 prompt·completion을 공개했지만, 폐쇄형 API의 내부 checkpoint를 공개하거나 미래에도 같은 version을 유지하게 만들지는 못했다.

### 비교를 통한 해석

네 자료를 함께 놓으면 공개성은 단일 선이 아니라 **검사 가능성, 변형 가능성, 재배포 권리, 재현 조건, 실행 자원과 거버넌스 참여의 벡터**로 보는 편이 설명력이 높다. 공개 weight는 이 벡터의 여러 값을 개선하지만 모두를 최대로 만들지는 않는다.

이 관점에서 재현 가능성도 `예/아니오` 하나가 아니다. 고정 checkpoint의 추론을 다시 돌리는 일, 평가 절차를 다시 실행하는 일, 같은 관측을 얻는 일과 사전학습을 다시 수행하는 일은 서로 다른 증거를 요구한다. 특히 closed API의 raw output 공개는 감사 가능성을 높이지만 model identity를 고정하지 않고, open weight는 model identity를 고정하지만 원 data·compute가 없으면 training lineage를 완전히 재현하지 못한다.

### 아직 입증되지 않은 계보와 인과

이 비교는 The Pile이 파운데이션 모델 보고서를 직접 낳았거나, 그 보고서가 BLOOM과 HELM을 단선적으로 설계하게 했다는 계보를 주장하지 않는다. 네 프로젝트는 일부 연구 공동체와 문제의식을 공유하지만 직접 영향에는 명시적인 설계 기록·인용과 참여자 증언이 더 필요하다.

또한 공개 weight가 항상 더 안전하거나 더 공정한 model을 만든다는 인과, RAIL의 이용 제한이 실제 오용을 줄였다는 효과, gated data access가 권리·대표성 문제를 해결했다는 결과는 현재 근거로 확인되지 않는다. BigScience의 큰 참여 규모가 의사 결정권과 보상을 균등하게 분배했다거나, HELM의 공개 결과가 모든 model provider의 투명성을 높였다는 광범위한 사회적 효과도 별도 연구가 필요하다.

마지막으로 exact retraining만을 재현성의 유일한 기준으로 삼지 않는다. 제한된 compute에서 weight·code·output을 사용해 독립 검증을 수행하는 가치와, 원 실험을 처음부터 같은 조건으로 반복하는 가치는 모두 중요하지만 서로 다른 성취다.

## 학습 확인

### 확인 질문

1. BLOOM weight와 code를 받을 수 있어도 ROOTS 전체와 384개 A100급 계산 조건이 없으면 exact retraining을 재현했다고 말할 수 없는 이유는 무엇인가?
2. The Pile의 project-level license와 component 원문의 권리 상태를 나눠 기록해야 하는 이유는 무엇인가?
3. HELM의 scenario·prompt·completion 공개가 폐쇄형 API 평가를 감사하게 하면서도 과거 model version 자체를 재현하지는 못하는 이유는 무엇인가?

### 다음 문서

- [[082_BLOOM과 공개 접근 다국어 LLM]] — BLOOM·ROOTS·RAIL의 artifact별 공개 조건과 계산 장벽을 1차 자료에 따라 확인한다.
- [[BLOOM]] — 공개 접근, architecture, 다국어 corpus와 governance를 하나의 model 개념 안에서 다시 연결한다.
- [[079_HELM과 다차원 언어 모델 평가]] — 평가 절차·산출물 공개와 변하는 API의 경계를 구체적으로 살핀다.
- [[데이터 품질과 분포 다양성은 같은 축인가]] — 공개 여부와 별개로 학습 data의 filtering·coverage·weight·권리 장부를 나눈다.

## 출처

- Leo Gao 외, [The Pile: An 800GB Dataset of Diverse Text for Language Modeling](https://arxiv.org/abs/2101.00027), 2020/2021; §§1.1·3–4·6–7, Tables 1·5, Appendices C–D.
- Stella Biderman, Kieran Bicheno, Leo Gao, [Datasheet for the Pile](https://arxiv.org/abs/2201.07311), 2022; pp. 1·8–17.
- Rishi Bommasani 외, [On the Opportunities and Risks of Foundation Models](https://crfm.stanford.edu/report), 2021; §§1.3–1.4·4.4·5.1·5.3·5.6.
- BigScience Workshop, [BLOOM: A 176B-Parameter Open-Access Multilingual Language Model](https://arxiv.org/abs/2211.05100), 2022; §§2.2·3.1–3.5·4.2–4.9·5, Tables 1·3–4.
- Hugo Laurençon 외, [The BigScience ROOTS Corpus: A 1.6TB Composite Multilingual Dataset](https://arxiv.org/abs/2303.03915), 2023; §§1–5, Appendix A.
- BigScience, [BigScience RAIL License v1.0](https://huggingface.co/spaces/bigscience/license/raw/main/BLOOMLICENSE.txt), 2022; Preamble, §I.2·6, §§II–III, Attachment A.
- Percy Liang 외, [Holistic Evaluation of Language Models](https://openreview.net/forum?id=iO4LZibEqW), TMLR, 2023; §§1.1–1.2·3–5·8·10–11, Tables 4–6·8·13, Appendices C·F–J.
- 프로젝트 검증 소스: [[074_The Pile과 대규모 언어 모델 학습 말뭉치]], [[076_파운데이션 모델 보고서와 AI 생태계]], [[079_HELM과 다차원 언어 모델 평가]], [[082_BLOOM과 공개 접근 다국어 LLM]].

## 관련 항목

- [[The Pile]]
- [[파운데이션 모델]]
- [[HELM]]
- [[BLOOM]]
- [[074_The Pile과 대규모 언어 모델 학습 말뭉치]]
- [[076_파운데이션 모델 보고서와 AI 생태계]]
- [[079_HELM과 다차원 언어 모델 평가]]
- [[082_BLOOM과 공개 접근 다국어 LLM]]
- [[데이터 품질과 분포 다양성은 같은 축인가]]
