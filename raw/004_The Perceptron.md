---
source_file: "004_The Perceptron.md"
translation_type: "한국어 번역"
source_stem: "004_The Perceptron"
order_prefix: "004"
topic: "The Perceptron"
tags:
  - LLM
  - NLP
  - AI-history
  - machine-learning
  - neural-networks
---

# 퍼셉트론 - 현대 신경망의 토대

1958년 Frank Rosenblatt는 Cornell Aeronautical Laboratory에서 퍼셉트론(perceptron)을 만들었다. 퍼셉트론은 패턴을 실제로 학습해 분류할 수 있었던 최초의 인공 신경망이었다. 이 획기적인 알고리즘은 기계가 경직된 규칙을 따르기만 하는 것이 아니라 예시로부터 학습할 수 있음을 입증했다. 또한 현대 딥러닝과 오늘날 우리가 사용하는 모든 신경망의 토대를 세웠다.

# 1958년: 퍼셉트론

1958년 Cornell Aeronautical Laboratory의 Frank Rosenblatt는 현대 신경망의 토대가 될 혁명적 기계 학습 알고리즘인 퍼셉트론을 소개했다. 이 성취는 컴퓨팅 기술의 점진적 발전을 훨씬 넘어서는 것이었다. 역사상 처음으로 기계는 가능한 모든 상황에 대해 명시적으로 프로그래밍되지 않고도, 예시로부터 패턴을 인식하는 법을 학습할 수 있었다. 그 함의는 깊었고, 인공지능 연구의 전체 궤적을 다시 형성하게 된다.

Rosenblatt의 연구는 풍부한 지적 전통에서 나왔다. 1943년 인공 뉴런의 수학적 모델을 제안했던 Warren McCulloch와 Walter Pitts의 초기 이론 작업을 바탕으로, Rosenblatt는 추상 이론을 실용적 현실로 바꾸었다. 그의 퍼셉트론은 단순한 개념 모델이 아니라 훈련하고, 테스트하고, 실제 문제 해결에 배치할 수 있는 작동 시스템이었다. 이론적 가능성과 실용적 구현 사이의 이런 차이는 이 분야의 분수령이 되었다.

Cornell Aeronautical Laboratory에서 Rosenblatt의 연구가 이루어진 맥락은 그 연구의 야심과 긴급성을 함께 설명해준다. 냉전기는 인간 운용자보다 더 빠르고 안정적으로 정보를 처리할 수 있는 자동 시스템에 대한 강한 수요를 만들었다. 군사 계획 담당자들은 항공사진에서 항공기, 전차, 기타 표적을 식별할 수 있는 패턴 인식 시스템을 필요로 했다. 분석가들이 모든 이미지를 수작업으로 검토하지 않고도 작동하는 시스템이 필요했던 것이다. 퍼셉트론은 라벨이 붙은 예시에서 직접 학습하여 서로 다른 표적 유형을 구별하는 시각적 패턴을 자동으로 발견함으로써 이 필요를 해결할 수 있을 것으로 기대되었다.

이 돌파구는 연구자들이 인공지능에 접근하는 방식의 근본적 전환을 뜻했다. 이전 시스템은 전적으로 손으로 코딩한 규칙과 논리 연산에 의존했으며, 프로그래머가 가능한 모든 상황을 예상하고 적절한 반응을 명시해야 했다. 반면 퍼셉트론은 기계가 경험을 통해 적응하고 성능을 개선할 수 있음을 보여주었다. 연구자들은 인간 전문 지식을 프로그램 논리에 직접 인코딩하는 대신 예시를 제공하고, 시스템이 패턴을 자율적으로 발견하게 할 수 있었다. 규칙으로 프로그래밍되는 대신 데이터에서 학습하는 이 패러다임은 결국 현대 AI의 지배적 접근이 된다.

이 발전의 의미는 직접적 응용을 훨씬 넘어선다. 퍼셉트론은 컴퓨터과학 안에서 기계 학습(machine learning)이 독립된 분야로 태어나는 계기를 만들었다. 표현(representation), 학습, 일반화에 관한 기본 원리를 세웠고, 이 원리들은 오늘날에도 연구를 이끈다. Rosenblatt가 개발한 수학적 틀, 반복적 오류 수정을 통해 연결 가중치를 조정한다는 생각, 훈련과 테스트를 통한 경험적 검증의 강조는 모두 이 선구적 작업에 뿌리를 둔다. 현대 딥러닝 시스템과 오늘날 언어 모델을 구동하는 신경망은 Rosenblatt의 퍼셉트론의 직접적 후손이며, 그가 1958년에 세운 동일한 기본 원리 위에 구축되어 있다.

## 퍼셉트론이란 무엇인가?

가장 근본적인 수준에서 퍼셉트론은 두 범주 사이의 최적 결정 경계(decision boundary)를 찾아 데이터를 두 범주로 나누는 법을 학습하는 선형 분류기(linear classifier)다. 이 설명은 기술적으로 정확하지만, 퍼셉트론이 나타낸 개념적 돌파구를 충분히 드러내지는 못한다. 퍼셉트론은 계산 자체에 대한 새로운 사고방식, 즉 기계가 미리 정해진 명령을 실행하기만 하는 것이 아니라 경험에서 학습할 수 있다는 사고방식을 구현했다.

퍼셉트론의 생물학적 영감은 그 설계를 이해하는 데 중요한 맥락을 제공한다. Rosenblatt는 뇌의 뉴런이 정보를 처리하는 방식과 명시적으로 평행 관계를 그렸다. 생물학적 뉴런은 가지 모양의 수상돌기(dendrite)를 통해 전기 신호를 받고, 세포체에서 이 신호를 통합하며, 결합된 자극이 특정 임계값을 넘으면 축삭(axon)을 따라 다른 뉴런으로 출력 신호를 발화한다. 퍼셉트론은 이 과정을 단순화된 수학적 형태로 모방한다. 여러 수치 입력을 받고, 각 입력에 중요도 가중치를 부여하며, 이 가중 입력들을 합친 뒤, 전체가 임계값을 넘는지에 따라 이진 출력을 낸다.

그러나 진정한 혁신은 이 구조 자체가 아니라 퍼셉트론의 학습 능력에 있었다. 시스템은 훈련 중에 저지른 실수에 따라 가중치(weights)라고 불리는 내부 매개변수를 자동으로 조정할 수 있었다. 퍼셉트론이 훈련 예시를 잘못 분류하면, 앞으로 같은 오류를 낼 가능성을 줄이도록 체계적으로 가중치를 수정했다. 퍼셉트론 학습 규칙(perceptron learning rule)으로 정식화된 이 학습 과정은 인간 개입 없이 기계가 성능을 개선할 수 있는 알고리즘적이고 재현 가능한 방법을 제공했다는 점에서 혁명적이었다.

실무적으로 이것이 무엇을 뜻했는지 생각해보자. 이전의 패턴 인식 시스템은 엔지니어가 의사결정 규칙을 수작업으로 지정해야 했다. 손글씨 숫자를 구별하는 시스템을 만들고 싶다면, 닫힌 고리의 개수, 가로선이나 세로선의 존재, 기타 손으로 설계한 특징에 관한 규칙을 프로그래밍해야 했을 것이다. 각 규칙은 명시적으로 코딩되고, 디버깅되고, 시행착오를 거쳐 다듬어져야 했다. 퍼셉트론은 이 고된 과정을 제거했다. 규칙을 지정하는 대신 라벨이 붙은 예시를 제공하면 되었다. 알고리즘은 데이터의 패턴을 자동으로 발견하고, 올바른 분류에 얼마나 유용한지에 따라 각 특징에 서로 다른 중요도를 부여하는 법을 학습했다.

퍼셉트론 구조의 우아함은 그 영향에 크게 기여했다. 여러 입력 신호가 가중 연결을 통해 결합되고, 입력과 독립적으로 조정할 수 있게 해주는 bias 항과 함께 합산되며, 단순한 임계값 함수를 통과해 이진 결정을 만든다. 이런 계산적 단순성 덕분에 퍼셉트론은 1950년대 하드웨어에서 실제 구현이 가능했고, 동시에 수학적으로 분석하기에도 다루기 쉬웠다. 연구자들은 퍼셉트론의 학습 능력에 관한 형식적 성질을 증명할 수 있었고, 언제 성공적으로 해를 찾을 수 있는지에 관한 보장을 세울 수 있었다. 실용적 유용성과 이론적 이해의 결합은 기계 학습을 공학적 휴리스틱이 아니라 엄밀한 과학 분야로 세우는 데 도움을 주었다.

Rosenblatt는 생물학적 뉴런에서 명시적으로 영감을 얻었다. 생물학적 뉴런은 수상돌기에서 신호를 받고, 세포체에서 이를 통합하며, 활성화가 임계값을 넘으면 축삭을 통해 출력 신호를 발화한다.

## 작동 방식

퍼셉트론은 수치 입력을 이진 분류로 바꾸는 단순하지만 강력한 과정을 통해 작동한다. 개념적으로는 단순하지만, 퍼셉트론이 정확히 어떻게 기능하는지 이해하려면 수학적 토대와 학습 절차를 함께 살펴보아야 한다. 예측을 만드는 순전파 계산과 성능을 개선하는 후방 조정이라는 두 측면이 함께 작동하면서, 경험에서 학습할 수 있는 시스템을 만든다.

### 수학적 모델

퍼셉트론의 핵심은 입력의 가중합을 계산하고, 여기에 임계값 함수를 적용해 출력을 만드는 것이다. 이 수학적 정식화는 정밀성과 일반성을 함께 제공하여, 동일한 기본 메커니즘을 매우 다양한 문제 영역에 적용할 수 있게 한다.

입력 집합 \(x_1, x_2, \ldots, x_n\)에 대해 퍼셉트론은 다음 식으로 출력을 계산한다.

```math
y = f\left(\sum_{i=1}^{n} w_i x_i + b\right)
```

이 식에서 \(w_i\)는 각 입력 \(x_i\)에 연결된 가중치를 나타내고, \(b\)는 bias 항이며, \(f\)는 임계값 활성화 함수(threshold activation function)다. 가중치는 각 입력이 최종 결정에 얼마나 영향을 미치는지를 정한다. 큰 양의 가중치를 가진 입력은 퍼셉트론이 1을 출력하도록 밀어주고, 큰 음의 가중치를 가진 입력은 0을 출력하도록 밀어준다. bias 항은 입력값과 독립적으로 결정 임계값을 조정할 수 있게 해주는 특별한 역할을 한다. bias가 없다면 모든 입력이 0인 퍼셉트론은 무엇을 학습했든 항상 같은 출력을 낼 것이다.

임계값 함수 \(f\)는 가중합을 이진 결정으로 바꾼다.

```math
f(z) =
\begin{cases}
1 & \text{if } z \geq 0 \\
0 & \text{if } z < 0
\end{cases}
```

이 함수는 생물학적 뉴런의 발화 행동을 수학적 형태로 구현한다. 결합된 가중 입력이 임계값을 넘으면, 여기서는 bias가 실제 임계값을 처리하므로 0을 기준으로 표현된다. 퍼셉트론은 1을 출력하며 "발화"한다. 그렇지 않으면 비활성 상태로 남아 0을 출력한다.

이 수학적 모델의 기하학적 해석은 추가적인 통찰을 제공한다. 입력 \(x_1\)과 \(x_2\)가 있는 2차원에서 식 \(w_1x_1 + w_2x_2 + b = 0\)은 하나의 직선을 정의한다. 이 직선의 한쪽에 있는 점들은 1을 출력하고, 다른 쪽에 있는 점들은 0을 출력한다. 가중치 \(w_1\)과 \(w_2\)는 직선의 방향을 정하고, bias \(b\)는 위치를 정한다. 이 기하학적 관점에서 학습은 두 클래스의 훈련 예시를 올바르게 분리할 때까지 이 결정 경계를 조정하는 일이다.

### 오류 수정을 통한 학습

퍼셉트론은 오류 수정에 기반한 단순하지만 놀라울 정도로 효과적인 규칙을 사용해 학습한다. 이 학습 절차는 오늘날 지도 학습(supervised learning)이라고 부르는 것의 가장 이른 사례 중 하나다. 지도 학습에서는 시스템이 자신의 예측을 알려진 정답과 비교하고, 차이를 줄이도록 행동을 조정하면서 성능을 개선한다.

학습 과정은 여러 훈련 예시에 대해 반복되는 일련의 단계로 진행되며, 퍼셉트론이 만족스러운 성능에 도달할 때까지 계속된다.

**가중치를 무작위로 초기화**: 모든 가중치와 bias를 작은 무작위 값으로 설정하면서 과정이 시작된다. 무작위 초기화는 퍼셉트론이 특정 패턴에 대한 사전 편향을 갖고 출발하지 않게 해주며, 중립적인 출발점에서 데이터의 구조를 발견할 수 있게 한다.

**훈련 예시 제시**: 시스템은 입력 패턴과 그에 대한 올바른 분류 라벨을 받는다. 이 라벨이 붙은 예시는 퍼셉트론이 풀어야 할 문제인 입력 패턴과, 만들어내야 할 해답인 정답 라벨을 함께 제공한다.

**예측 생성**: 퍼셉트론은 현재 가중치를 사용해 주어진 입력에 대한 출력을 계산한다. 이 순전파(forward pass)는 앞에서 설명한 수학적 모델을 적용하여 가중합을 계산하고 임계값 함수에 통과시킨다.

**틀렸을 때 가중치 갱신**: 학습 메커니즘의 핵심이 여기에 있다. 퍼셉트론의 예측이 정답 라벨과 일치하면 아무 변화도 일어나지 않는다. 현재 가중치가 이미 이 예시에 대해 올바른 답을 만들기 때문이다. 그러나 예측이 정답과 다르면, 퍼셉트론은 오류를 줄이도록 체계적으로 가중치를 조정한다.

가중치 갱신 규칙은 단순하지만 정교한 학습 원리를 담고 있다. 각 가중치 \(w_i\)에 대한 갱신은 다음 식을 따른다.

```math
w_i^{new} = w_i^{old} + \eta \cdot (target - output) \cdot x_i
```

학습률 \(\eta\)(eta)는 퍼셉트론이 오류에 반응해 가중치를 얼마나 공격적으로 조정할지를 통제한다. 학습률이 크면 조정 폭이 커져 학습이 빨라질 수 있지만 불안정해질 위험도 커진다. 학습률이 작으면 더 조심스럽고 점진적으로 변해 안정적으로 수렴할 수 있지만 더 많은 훈련 반복이 필요하다.

\((target - output)\) 항은 오류를 정량화한다. 출력이 0과 1인 이진 분류(binary classification)에서 이 차이는 +1, -1, 또는 0이 될 수 있다. 퍼셉트론이 1을 출력해야 했는데 0을 출력하면 +1이고, 0을 출력해야 했는데 1을 출력하면 -1이며, 예측이 맞으면 0이다. 이 오류의 부호가 가중치가 증가할지 감소할지를 결정한다.

\(x_i\)를 곱하는 것은 중요한 원리를 구현한다. 활성 입력에 연결된 가중치는 비활성 입력에 연결된 가중치보다 더 크게 갱신된다. 어떤 입력이 강한 양의 값을 가졌고, 퍼셉트론이 1을 출력해야 하는데 잘못 0을 출력했다면, 그 입력의 가중치는 상당히 증가한다. 그러면 앞으로 그 입력이 활성화된 예시에서 퍼셉트론이 1을 출력할 가능성이 커진다. 반대로 어떤 입력이 0이라면 그 가중치는 전혀 변하지 않는다. 그 입력은 현재 결정에 아무 역할도 하지 않았으므로 오류의 책임을 질 이유가 없기 때문이다.

이 학습 절차는 훈련 예시들을 반복해서 순회하며 실수에 따라 가중치를 조정하고, 모든 훈련 예시를 올바르게 분류하거나 미리 정한 최대 반복 횟수에 도달할 때까지 계속된다. 이 접근의 우아함은 각 가중치 갱신이 쉽게 얻을 수 있는 국소 정보에만 의존한다는 단순성과, 클래스를 분리하는 결정 경계를 찾는 전역적 효과가 결합되어 있다는 데 있다.

### 실용 예시: 이메일 스팸 탐지

이 추상적 원리를 구체적으로 이해하기 위해, 퍼셉트론이 이메일을 스팸 또는 정상 메일로 분류하는 법을 학습하는 예시를 살펴보자. 이 예시는 순전파 계산과 학습 과정이 실제로 어떻게 작동하는지를 함께 보여준다.

각 이메일에서 추출한 세 가지 수치 특징을 사용해 스팸 이메일과 정상 이메일을 구별한다고 하자.

- \(x_1\): "free", "offer", "discount" 같은 홍보성 단어의 개수
- \(x_2\): "urgent", "act now", "limited time" 같은 긴급성 표현의 개수
- \(x_3\): 전체 글자 중 대문자 비율. 스팸은 과도한 대문자 사용이 잦다.

초기 가중치를 \(w_1 = 0.1\), \(w_2 = 0.2\), \(w_3 = 0.3\), bias를 \(b = -0.5\)로 시작한다고 하자. 이 시작값은 작고 본질적으로 임의적이며, 퍼셉트론은 학습을 통해 이를 조정한다.

**첫 번째 훈련 예시**: 특징이 \([2, 1, 0.1]\)이고 정답 라벨이 0, 즉 스팸이 아닌 정상 이메일을 생각해보자. 이 이메일은 특별 제안을 한두 번 언급하지만 전체적으로는 정상적인 서신에 가까울 수 있다.

예측을 만들기 위해 가중합을 계산한다.

```math
\begin{align*}
sum &= 0.1 \cdot 2 + 0.2 \cdot 1 + 0.3 \cdot 0.1 + (-0.5) \\
&= 0.2 + 0.2 + 0.03 - 0.5 \\
&= -0.07
\end{align*}
```

합은 -0.07로 0보다 작으므로 임계값 함수는 0을 출력한다. 퍼셉트론은 이 이메일을 정상 메일로 올바르게 분류했다. 예측이 정답 라벨과 일치하므로 가중치 갱신은 일어나지 않는다. 현재 가중치가 이 예시에 대해서는 이미 잘 작동한다.

**두 번째 훈련 예시**: 이제 특징이 \([8, 6, 0.4]\)이고 정답 라벨이 1, 즉 스팸인 이메일을 생각해보자. 이 이메일은 홍보성 단어가 많고, 긴급성 표현도 여러 개 있으며, 상당한 대문자 사용을 보인다.

가중합은 다음과 같다.

```math
\begin{align*}
sum &= 0.1 \cdot 8 + 0.2 \cdot 6 + 0.3 \cdot 0.4 + (-0.5) \\
&= 0.8 + 1.2 + 0.12 - 0.5 \\
&= 1.62
\end{align*}
```

합 1.62는 0을 넘으므로 임계값 함수는 1을 출력한다. 퍼셉트론은 이를 스팸으로 올바르게 식별했다. 이번에도 가중치 갱신은 필요하지 않다.

**실수에서 학습하기**: 흥미로운 경우는 퍼셉트론이 오류를 낼 때다. 특징이 \([5, 3, 0.3]\)이고 정답 라벨이 1인 스팸 이메일을 생각해보자. 현재 가중치로 가중합을 계산하면 다음과 같다.

```math
\begin{align*}
sum &= 0.1 \cdot 5 + 0.2 \cdot 3 + 0.3 \cdot 0.3 + (-0.5) \\
&= 0.5 + 0.6 + 0.09 - 0.5 \\
&= 0.69
\end{align*}
```

잠깐 다시 생각해보자. 합이 0.69라면 양수이므로 출력은 1이 되고, 이는 정답이다. 이 설명을 위해서는 대신 가중치와 bias의 조합이 음의 합을 만들어 퍼셉트론이 1을 출력해야 할 때 잘못 0을 출력했다고 가정하자.

이 오분류가 발생하면 퍼셉트론은 가중치 갱신 규칙을 적용한다. 학습률 \(\eta = 0.1\)을 사용하면 오류 항은 \((1 - 0) = 1\)이고, 새 가중치는 다음과 같이 계산된다.

```math
w_1^{new} = 0.1 + 0.1 \cdot 1 \cdot 5 = 0.6
```

```math
w_2^{new} = 0.2 + 0.1 \cdot 1 \cdot 3 = 0.5
```

```math
w_3^{new} = 0.3 + 0.1 \cdot 1 \cdot 0.3 = 0.33
```

각 가중치에 무슨 일이 일어났는지 주목하자. 홍보성 단어 수를 세는 특징에 연결된 첫 번째 가중치는 그 특징값이 5로 비교적 컸기 때문에 크게 증가했다. 두 번째 가중치는 긴급 표현 개수 3에 대응하여 중간 정도로 증가했다. 세 번째 가중치는 대문자 비율 0.3이 결정에 작게 기여했으므로 거의 변하지 않았다. 이런 갱신은 앞으로 비슷한 패턴을 가진 이메일에서 퍼셉트론이 1을 출력하도록 밀어준다.

이 반복 과정은 계속된다. 훈련 예시들을 순회하고 각 실수 뒤에 가중치를 조정하면서, 퍼셉트론은 모든 훈련 예시를 올바르게 분류하거나 미리 정한 최대 반복 횟수에 도달할 때까지 학습한다. 예시에 반복적으로 노출되고 체계적으로 오류를 수정함으로써, 퍼셉트론은 점차 스팸 이메일과 정상 이메일을 구별하는 법을 익힌다.

Rosenblatt는 데이터가 선형 분리 가능(linearly separable)하다면 퍼셉트론 학습 알고리즘이 유한 시간 안에 해를 찾는다는 것을 증명했다.

## 퍼셉트론이 가능하게 한 것

퍼셉트론의 등장은 여러 영역에서 자동 패턴 인식과 기계 학습 응용의 완전히 새로운 가능성을 열었다. 퍼셉트론이 가능하게 한 것을 이해하려면 직접적인 실용 응용뿐 아니라, 연구자들이 인공지능 문제에 접근하는 방식에서 나타낸 더 넓은 개념적 전환도 함께 보아야 한다.

군사 및 방위 응용에서 퍼셉트론은 정보 분석 작업 흐름을 근본적으로 바꿀 수 있는 자동 표적 인식 시스템 개발을 가능하게 했다. 이전에는 항공 및 위성 이미지에서 항공기, 선박, 전차, 기타 군사 자산을 식별하려면 훈련된 인간 분석가 팀이 사진을 수작업으로 검토해야 했다. 이 과정은 시간이 많이 들고 비용이 컸으며, 인간의 오류와 피로에 영향을 받았다. 퍼셉트론 기반 시스템은 서로 다른 표적 유형과 관련된 특징적 시각 패턴을 학습해, 인간 분석가보다 훨씬 빠르게 이미지를 처리하면서도 긴 시간 동안 일관된 성능을 유지할 수 있었다. 냉전기에는 군사 자산의 빠른 식별이 중요한 정보 우위를 제공할 수 있었기 때문에 전략적 함의가 컸다.

퍼셉트론은 군사적·상업적 응용을 모두 가진 문제인 광학 문자 인식(optical character recognition, OCR)에서 특히 가치가 있었다. 초기 OCR 시스템은 픽셀 표현을 바탕으로 서로 다른 글자와 숫자를 구별하는 데 퍼셉트론을 사용했다. 퍼셉트론은 어둡고 밝은 픽셀의 특정 패턴이 특정 문자에 대응한다는 것을 학습할 수 있었으며, 문자의 크기, 글꼴, 품질이 달라져도 이를 인식할 수 있었다. 당시로서는 이런 시스템들이 놀라운 정확도를 달성했고, 특정 시각 인식 과제에서 기계가 인간 성능에 맞먹거나 이를 넘어설 수 있음을 보여주었다. 이 능력은 타자 및 인쇄 문서의 자동 처리를 가능하게 하여, 수작업 데이터 입력의 필요를 줄이고 자동 우편 분류, 은행 수표 처리, 문서 디지털화의 가능성을 열었다.

산업 응용도 퍼셉트론이 실질적 가치를 보여준 중요한 영역이었다. 퍼셉트론 학습에 기반한 품질 관리 시스템은 조립 라인에서 결함 제품을 자동으로 탐지할 수 있었다. 제조 기업은 양품과 불량품의 예시를 제공해 이런 시스템을 훈련할 수 있었다. 퍼셉트론은 품질 문제를 나타내는 미묘한 시각적 또는 센서 기반 패턴을 구별하는 법을 학습했다. 이 자동화는 검사 비용을 크게 줄이면서 일관된 품질 기준을 유지했다. 퍼셉트론 기반 시스템은 인간 검사자와 달리 피로나 주의력 저하를 겪지 않기 때문이다.

퍼셉트론 알고리즘의 계산 효율성은 실제 성공에서 중요한 역할을 했다. 학습 절차는 덧셈, 곱셈, 비교라는 기본 산술 연산만을 필요로 했고, 그래서 1950년대와 1960년대의 제한된 컴퓨팅 하드웨어에서도 구현할 수 있었다. 방대한 프로그래밍이 필요하고 많은 계산 자원을 소비하는 복잡한 규칙 기반 시스템과 달리, 퍼셉트론은 비교적 빠르게 훈련되고 효율적으로 배치될 수 있었다. 이런 접근성은 더 다양한 기관과 연구자들이 자동 학습 시스템을 실험할 수 있게 하여 기계 학습 연구의 저변을 넓혔다.

무엇보다도 퍼셉트론은 라벨이 붙은 예시에서 학습하는 지도 학습이라는, 이후 인공지능 연구를 지배하게 될 기본 패러다임을 세웠다. 프로그래머가 규칙과 의사결정 논리를 명시적으로 인코딩해야 하는 대신, 퍼셉트론은 시스템이 훈련 데이터를 살펴보며 패턴을 자율적으로 발견할 수 있음을 보여주었다. 지식 공학에서 데이터 기반 학습으로의 이 전환은 방법론상의 깊은 변화였다. 그것은 인공지능에 이르는 길이 기계에 인간 지식을 프로그래밍하는 것이 아니라, 데이터를 제공하고 학습을 통해 패턴을 추출하게 하는 데 있을 수 있음을 시사했다. 퍼셉트론의 제약 때문에 제한적이긴 했지만, 이 개념은 이후 거의 모든 기계 학습 발전의 토대가 되었고 현대 딥러닝 접근의 기반으로 남아 있다.

혁명적 성격에도 불구하고 원래의 퍼셉트론은 선형 분리 가능한 패턴만 학습할 수 있었기 때문에, 비교적 단순한 분류 문제로 적용 범위가 제한되었다.

## 한계

획기적 성격과 실용적 성공에도 불구하고, 퍼셉트론은 더 넓은 채택을 제한하고 이 접근의 근본적 한계를 드러낸 몇 가지 중대한 제약에 직면했다. 이 한계를 이해하는 일은 퍼셉트론의 역사적 영향과, 이후 이런 결함을 해결한 신경망 연구의 발전을 함께 평가하는 데 중요하다.

가장 근본적인 한계는 퍼셉트론이 선형 분리 가능한 문제에 제한된다는 점이었다. 이 제약은 퍼셉트론이 두 클래스의 예시를 완벽히 분리할 수 있는 직선, 3차원에서는 평면, 더 높은 차원에서는 초평면이 존재하는 분류 과제만 성공적으로 학습할 수 있음을 뜻했다. 이는 많은 유용한 응용을 포함하지만, 입력과 출력 사이에 복잡한 비선형 관계가 자주 나타나는 실제 문제에서는 심각한 제한이다.

Marvin Minsky와 Seymour Papert가 1969년 책 *Perceptrons*에서 널리 주목하게 만든 유명한 XOR 문제는 이 근본적 한계를 강렬하게 보여주었다. XOR(exclusive-or) 함수는 가장 단순한 비선형 논리 연산 가운데 하나다. 두 이진 입력이 서로 다를 때, 즉 (0,1) 또는 (1,0)일 때 1을 출력하고, 두 입력이 같을 때, 즉 (0,0) 또는 (1,1)일 때 0을 출력한다. 겉보기에는 단순하지만, 단일 직선으로는 XOR 사례를 올바르게 분리할 수 없다. 네 가지 가능한 입력 조합을 2차원 평면에 표시하면, XOR이 1을 출력해야 하는 두 경우는 서로 대각선 반대편에 있고, 0을 출력해야 하는 두 경우도 다른 대각선 위치에 있다. 한 쌍을 올바르게 분리하는 어떤 직선도 다른 쌍은 잘못 분류한다. 이 기하학적 불가능성은 기본 퍼셉트론이 얼마나 오래 훈련하든, 학습률을 어떻게 조정하든 XOR 함수를 결코 학습할 수 없음을 뜻했다. 이렇게 단순한 논리 연산조차 퍼셉트론의 능력 밖에 있다는 사실은 깊은 이론적 제약을 드러냈고, 1970년대 내내 신경망 연구에 대한 열기를 식게 만들었다.

훈련 안정성도 실제 응용을 복잡하게 만든 추가 과제였다. 데이터가 선형 분리 가능하지 않을 때 퍼셉트론 학습 알고리즘은 안정적인 답으로 수렴하지 못하고 여러 해 사이를 진동할 수 있었다. 알고리즘은 어떤 예시 집합을 올바르게 분류하도록 가중치를 조정할 수 있지만, 그 조정이 다른 예시에서 오류를 일으킬 수 있다. 새 오류를 고치려 하면 다시 원래의 실수가 되살아나 끝없는 순환이 생길 수 있다. 이런 불안정성은 측정 오류나 일관되지 않은 라벨링이 훈련 집합에 모순처럼 보이는 사례를 도입하는 잡음 많은 데이터, 또는 서로 다른 범주의 예시가 비슷한 특징값을 공유하는 겹치는 클래스에서 특히 문제가 되었다.

학습률 매개변수는 신중한 수동 조정을 필요로 했고, 이는 퍼셉트론 훈련에 또 다른 어려움을 더했다. 학습률을 너무 높게 설정하면 알고리즘이 크고 공격적인 가중치 조정을 하여 최적해를 지나치고 불안정해질 수 있다. 퍼셉트론은 해 공간을 이리저리 튕기듯 움직이며 좋은 구성에 정착하지 못할 수 있다. 반대로 학습률을 너무 낮게 설정하면 조심스럽고 점진적인 가중치 변화 때문에 학습이 극도로 느리게 진행되어, 받아들일 만한 성능에 도달하기까지 훨씬 많은 훈련 반복이 필요하다. 적절한 균형을 찾으려면 흔히 많은 실험과 영역 전문성이 필요했다.

이진 임계값 활성화 함수는 확신도나 불확실성에 대한 어떤 정보도 없이 딱딱한 분류만 출력한다는 또 다른 한계를 만들었다. 퍼셉트론은 항상 0 또는 1만 출력하며, 그 분류에 얼마나 확신하는지는 알려주지 않았다. 결정 경계 바로 한쪽에 있는 예시와 경계에서 멀리 떨어진 예시는 직관적으로 서로 다른 확신 수준을 가져야 하지만, 퍼셉트론은 둘 모두에 같은 확정적 분류를 부여했다. 현대의 확률적 접근은 확신 수준이나 확률로 해석할 수 있는 연속값을 출력함으로써 이 문제를 다룬다. 퍼셉트론에 불확실성 정량화가 없다는 점은 예측 신뢰도를 이해하는 것이 중요한 의료 진단이나 금융 의사결정 같은 응용에 덜 적합하게 만들었다. 이런 분야에서는 확신 수준에 따라 다른 행동이 적절할 수 있기 때문이다.

이런 한계들이 널리 이해되자, 1970년대에는 신경망에 대한 자금과 관심이 급격히 줄어드는 유명한 "AI 겨울(AI Winter)"이 찾아왔다.

연구자들이 퍼셉트론을 더 크고 복잡한 문제에 적용하려 하면서 확장성 문제도 나타났다. 단층 퍼셉트론은 근본적으로 계층적 패턴을 포착하거나 데이터의 중간 표현을 학습할 수 없었다. 많은 실제 문제에서 분류에 가장 유용한 특징은 원시 입력 그 자체가 아니라 그 입력에서 파생된 고수준 추상화다. 예를 들어 이미지 인식에서는 낮은 수준의 픽셀값이 결합해 에지를 만들고, 에지가 결합해 모양을 만들며, 모양이 결합해 인식 가능한 물체를 만든다. 단층 퍼셉트론 구조에는 이런 중간 표현을 학습할 메커니즘이 없었다. 모든 특징은 문제 영역을 충분히 잘 이해해 유익한 특성을 식별할 수 있는 인간 전문가가 수작업으로 설계해야 했다. 이런 수동 특징 공학(feature engineering) 요구는 퍼셉트론의 적용 가능성을 제한했고, 학습 접근이 제거하려 했던 인간 노동의 상당 부분을 다시 끌어들였다. 이 한계들의 많은 부분에 대한 해법은 결국 다층 신경망과 역전파(backpropagation) 알고리즘을 통해 등장하지만, 그런 발전은 훗날의 일이며 그 전에는 첫 번째 "AI 겨울"이라고 불리는 연구 관심 감소의 시기가 있었다.

## 유산과 현대적 영향

퍼셉트론의 영향은 1950년대와 1960년대의 직접적 응용을 훨씬 넘어 현대 인공지능에까지 이어진다. 퍼셉트론은 오늘날의 기계 학습 연구와 실천을 계속 형성하는 기본 원리를 세웠다. 1970년대에 신경망에 대한 관심이 줄어들게 만든 한계에도 불구하고, Rosenblatt의 작업에서 나온 핵심 통찰은 오래도록 가치 있는 것으로 드러났고 이후 돌파구의 필수 구성 요소가 되었다.

퍼셉트론이 도입한 gradient 기반 학습 개념은 현대 신경망 훈련의 중추가 되었다. 퍼셉트론 학습 규칙 자체는 비교적 단순했지만, 네트워크 매개변수는 오류를 줄이는 방향으로 조정되어야 한다는 기본 원리를 담고 있었다. 이 생각은 더 정교한 최적화 알고리즘으로 발전했으며, 특히 역전파는 오류가 여러 층을 거슬러 어떻게 전파되어 네트워크 전체의 가중치를 갱신해야 하는지를 계산함으로써 gradient 기반 학습을 다층 네트워크로 확장했다. 수십억 개의 매개변수를 가진 네트워크를 훈련하는 오늘날의 딥러닝 시스템도, 퍼셉트론이 처음 보여준 반복적 gradient 기반 최적화라는 같은 기본 원리에 의존한다.

현대 신경망은 퍼셉트론 구조의 직접적 후손이다. 선형 분리 가능성 한계를 해결하기 위해 1980년대에 개발된 다층 퍼셉트론(multi-layer perceptron, MLP)은 Rosenblatt의 단층 설계를 여러 층으로 쌓고, 그 사이에 비선형 활성화 함수를 넣었다. 겉보기에는 단순한 이 구조 변화가 능력을 극적으로 바꾸어, 단층 네트워크로는 결코 포착할 수 없었던 복잡하고 계층적인 패턴을 학습할 수 있게 했다. 원래 퍼셉트론을 좌절시킨 XOR 문제는 단 하나의 은닉층만으로도 쉽게 해결된다. 수십 또는 수백 개의 층을 가진 오늘날의 심층 신경망은 Rosenblatt의 원래 설계에서 이어지는 직접적 진화 경로를 나타내며, 그가 세운 기본 뉴런 모델과 학습 원리를 확장한 것이다.

퍼셉트론이 개척한 지도 학습 패러다임, 즉 라벨이 붙은 예시에서 반복적 오류 수정을 통해 학습하는 방식은 현대 AI에서도 지배적인 훈련 방법론으로 남아 있다. 시스템에 입력-출력 쌍을 제공하고, gradient descent를 통해 패턴을 발견하게 하는 이 접근은 사실상 모든 현대 기계 학습 응용의 바탕에 놓여 있다. GPT, BERT와 그 후속 모델 같은 현재의 언어 모델도 훨씬 정교한 구조, 더 큰 데이터셋, 더 많은 계산 자원을 사용하지만 동일한 기본 접근을 활용한다. 지난 10년간 자연어 처리에서 일어난 혁명은 Rosenblatt가 도입한 지도 학습 틀 위에 직접 구축되어 있다.

현대 언어 AI 시스템에서도 본질적으로 퍼셉트론처럼 기능하는 구성 요소는 여전히 중요한 역할을 한다. 신경망의 선형 층(linear layer), 특히 최종 예측을 만드는 출력층은 원래 퍼셉트론을 특징짓던 가중합 더하기 bias 계산을 구현한다. 이 층들은 더 깊은 층에서 학습된 표현을 받아 최종 출력을 만들기 위해 선형 분류를 수행한다. 퍼셉트론 계산을 정의하는 수학적 정식화 \(y = f(Wx + b)\)는 현대 구조 곳곳에 등장한다.

퍼셉트론의 유산은 신경망을 넘어 다른 기계 학습 접근에도 이어진다. 1990년대에 개발된 서포트 벡터 머신(support vector machine, SVM)은 선형 분류에 대한 퍼셉트론의 기하학적 해석 위에 직접 구축되었다. SVM은 클래스 사이의 margin을 최대화하여 최적의 선형 분리자를 찾는데, 이는 임의의 선형 분리자를 찾는 퍼셉트론 접근을 정교화한 것이다. 커널 방법(kernel method)은 데이터를 암묵적으로 더 높은 차원 공간으로 변환해 선형 분리가 가능하게 함으로써 이 틀을 비선형 문제로 확장했다. 이는 퍼셉트론의 핵심 원리를 보존하면서 그 근본 한계 중 하나를 다룬 것이다.

무엇보다도 퍼셉트론은 기계 학습을 이론적 토대와 실용적 응용을 모두 갖춘 엄밀한 과학 분야로 세우는 데 도움을 주었다. 선형 분리 가능한 데이터에 대해 퍼셉트론이 유한 시간 안에 해를 찾는다는 Rosenblatt의 수렴 정리는, 학습 알고리즘의 성질을 수학적으로 분석하고 형식적으로 증명할 수 있음을 보여주었다. 실용적 유용성과 이론적 이해의 결합은 이후 기계 학습 연구의 기준을 세웠다. 퍼셉트론은 자동 학습이 단순한 공학적 요령이 아니라 체계적 과학 탐구를 통해 이해하고, 분석하고, 개선할 수 있는 현상임을 보여주었다. 경험적 성공과 이론적 통찰을 결합하는 이 유산은 오늘날 기계 학습 연구의 중요한 특징으로 남아 있다.

오늘날 가장 발전한 Transformer 구조에도 본질적으로 퍼셉트론처럼 기능하는 선형 층이 포함되어 있다. 이는 Rosenblatt의 기본 통찰이 여전히 중요하다는 점을 보여준다.

손으로 코딩한 규칙이 아니라 데이터에서 학습한다는 퍼셉트론의 강조는 기계 학습을 컴퓨터과학과 인공지능 안의 독립된 분야로 세웠다. 이 패러다임 전환은 여러 세대의 연구자들에게 영향을 미쳤고, 오늘날의 데이터 기반 AI 시스템 시대로 직접 이어졌다. Rosenblatt의 원래 퍼셉트론에는 중대한 한계가 있었지만, 그것이 구현한 원리, 즉 오류 수정을 통한 학습, gradient descent를 통한 매개변수 조정, 단순한 단위들의 네트워크로 계산을 표현한다는 생각은 인공지능 역사에서 가장 중요한 아이디어들 가운데 하나로 남았다. 현대 신경망이 데이터를 학습해 성능을 개선할 때마다, 그것은 Frank Rosenblatt가 1958년에 보여준 근본 통찰을 되풀이한다. 기계는 학습할 수 있다.
